# 开发记录

## 2026-09-23 腾讯云服务器部署与域名备案边界

- 任务：将当前 GitHub 项目部署到用户腾讯云轻量应用服务器，并配置 `jindashi.cc` 域名解析。
- 主要实现：在腾讯云轻量应用服务器 `106.53.200.63` 安装 Node.js、Git、Nginx，克隆 `https://github.com/zzhaozi58/master.git` 到 `/opt/jindashi/app`；配置 systemd 服务 `jindashi.service` 运行 `backend/server.js`；配置 Nginx 静态页面与 `/health`、`/auth`、`/customer`、`/master`、`/admin`、`/media` 反向代理。
- 域名配置：在腾讯云 DNSPod 为 `jindashi.cc` 和 `www.jindashi.cc` 添加 A 记录，均指向 `106.53.200.63`。
- 重要边界：服务器位于腾讯云中国内地地域，域名正式访问必须先完成 ICP 备案或接入备案；当前域名请求会被腾讯云未备案拦截页面阻断，公网 IP 访问可用于部署验证。
- 验证：服务器端 `npm run verify` 通过；服务器内侧 `/health` 返回 200；本机访问 `http://106.53.200.63/health` 返回 `{"ok":true,"data":{"status":"ok"}}`，访问 `http://106.53.200.63/` 返回金大师原型首页。

## 2026-09-13 金大师补漆维修小程序 MVP HTML 原型

- 任务：根据 `/Users/zhanghao/Documents/金大师` 中的宣传单、报价表和 Logo，制作单页移动端 HTML 高保真原型。
- 内容来源：价格表来自 `家具修复上门报价表.docx`；服务方式、保障与流程来自两张宣传单图片。
- 主要实现：品牌头部、参考价格表、服务方式/保障、维修需求表单；包含图片/视频上传、材质单选、修复类型多选、期望上门时间、预计工期、金/银/铜师傅人数、联系人信息与底部提交 CTA。
- 设计决策：移动端优先，采用宣传单中的深绿、金色和白底体系；价格只展示素材明确提供的项目，复杂情况保留现场报价/面议说明。
- 交互验证：在 390px 宽度下检查页面整体无横向溢出；测试空表单校验、文件上传预览、单选/多选、师傅步进器、完整提交成功反馈。
- 未决事项：当前为静态 MVP 原型，提交结果仅在页面内反馈并打印到浏览器控制台，尚未接入小程序后端或真实订单系统。

## 2026-09-14 增加底部 Tab、订单验收和我的页面

- 任务：将原单页原型扩展为更接近微信小程序的信息架构，增加底部三栏导航。
- 主要实现：底部 Tab 包含“询价 / 验收 / 我的”；询价页保留价格、服务介绍和维修需求表单；验收页支持待验收订单展示、上传图片/视频和填写问题说明；我的页支持名字/公司名称、微信号/微信昵称、联系电话维护，展示订单历史与联系客服入口。
- 设计决策：提交询价 CTA 移入询价页表单底部，底部固定区域用于小程序 Tab 导航，避免和多页面结构冲突。
- 交互验证：在 390px 宽度下验证 Tab 切换、验收问题校验与提交、验收上传预览、我的信息保存、联系客服反馈；页面整体无横向溢出，浏览器控制台无错误。
- 未决事项：订单数据、客服入口和资料保存仍为静态原型行为，正式小程序需接入真实登录、订单、上传和客服能力。

## 2026-09-14 验收页支持多个待验收订单切换

- 任务：当存在多个待验收订单时，在验收页“待验收订单”区使用顶部订单 Tab 切换。
- 主要实现：新增横向订单 Tab，提供 3 个待验收订单示例；点击 Tab 后同步更新订单标题、客户/师傅信息、维修内容、上门时间、当前状态和问题提交区的当前订单号。
- 交互验证：在 390px 宽度下验证第二个订单切换后，订单详情与提交区订单号同步；提交验收问题时控制台记录的订单号为当前选中订单；页面整体无横向溢出，控制台无错误。

## 2026-09-14 首页询价表增加伤口数量与长度

- 任务：在首页维修需求表单的“修复类型”下方增加伤口总数和伤口总长度字段。
- 主要实现：新增“伤口总数（处）”和“伤口总长度（厘米）”两个数字输入，纳入询价表单校验和提交数据。
- 交互验证：空表单会提示两个新增字段；完整提交时控制台记录包含 `damageCount` 和 `damageLength`；390px 宽度下页面整体无横向溢出，控制台无错误。

## 2026-09-14 调整修复类型、材质与验收交互

- 任务：修复类型增加缺角、裂缝、拼缝并移除断裂；修复材质增加水磨石；验收页增加通过/不通过选择，提交验收问题默认折叠。
- 主要实现：首页修复类型改为划痕、坑洞、缺角、裂缝、拼缝、补漆；材质增加水磨石；验收页默认选择通过验收，可直接确认通过，选择不通过时展开问题表单，也可点折叠条手动展开。
- 交互验证：390px 宽度下验证新增材质和修复类型；确认修复类型选项中不再包含断裂；验收页默认折叠，选择不通过后展开问题表单，选择通过后折叠并可确认通过；控制台无错误。

## 2026-09-14 材质和修复类型增加其他输入

- 任务：修复材质和修复类型都增加“其他”选项，并在同一行提供手动填写输入框。
- 主要实现：材质区新增“其他 + 请填写具体材质”，选择后作为单选材质；修复类型区新增“其他 + 请填写具体修复类型”，可与其他类型一起多选；提交时将“其他”替换为用户填写的具体内容；未选择其他时输入框折叠隐藏，再次点击其他可取消选择。
- 交互验证：390px 宽度下确认两个“其他”组合项默认只显示按钮且页面无横向溢出；选择其他后输入框展开，再次点击后取消选择并折叠；选择其他并填写“微水泥”“表面翻新”后完整提交成功，控制台提交数据包含手填内容且无错误。

## 2026-09-14 验收选择改为勾选样式

- 任务：验收页通过/不通过的 checkbox 使用勾号，不再显示正方形。
- 主要实现：将选中态伪元素改为 `✓`，未选中态不显示方块。
- 交互验证：390px 宽度下确认选中项显示 `✓`，未选中项不显示方块，页面整体无横向溢出且控制台无错误。

## 2026-09-14 不通过验收时凭证和说明必填

- 任务：选择不通过验收时，上传验收图片和视频、问题说明需要显示星号并作为必填项。
- 主要实现：问题表单中“上传验收图片和视频 *”提示为必填且至少 1 项，“问题说明 *”必填；提交验收问题时校验上传数量和文字说明。
- 交互验证：390px 宽度下选择不通过后问题表单展开，空提交同时提示上传凭证和问题说明；上传 1 张图片并填写说明后提交成功，控制台记录 `result: fail` 且 `filesCount: 1`，无控制台错误。

## 2026-09-14 管理端小程序 HTML 原型

- 任务：根据小程序端原型制作金大师补漆维修管理端原型，并按要求保持为小程序形态而非桌面后台。
- 主要实现：重做 `admin.html` 为移动端优先管理小程序，底部 Tab 包含“订单 / 验收 / 师傅 / 客户”；订单页支持状态筛选、搜索、订单详情、客户上传凭证占位、报价和派单操作；验收页承接不通过验收的问题、图片和视频凭证；师傅页展示金银铜等级、能力标签和接单状态；客户页展示公司名称、联系人、微信和订单历史。
- 设计决策：复用金大师深绿/金色品牌体系、Logo 和小程序式底部导航，控制在 430px 宽容器内，保持 B 端操作清晰但不过度桌面化。
- 交互验证：390px 宽度下验证初始布局、订单筛选、详情切换、保存报价、确认派单、四个底部 Tab 导航；页面无横向溢出，控制台无错误。

## 2026-09-14 管理端订单状态入口改为 Tab

- 任务：将管理端首页顶部四个数据入口改为“待报价 / 待派单 / 施工中 / 待验收”，保留数字并支持点击筛选。
- 主要实现：顶部四个数据块改为按钮式状态 Tab，默认选中待报价；点击后同步筛选订单列表并更新订单详情；新增 1 条待验收示例订单，确保第四个状态有对应内容。
- 交互验证：390px 宽度下验证四个状态入口均为 `button`，数字为 2/1/1/1；分别点击后订单列表和详情状态切换正确，页面无横向溢出。

## 2026-09-14 管理端订单详情改为二级页面

- 任务：订单详情不再直接显示在订单首页下方，改为点击订单后新开页面。
- 主要实现：新增 `orderDetail` 二级页面，订单卡点击后进入详情页；详情页顶部提供返回按钮，底部 Tab 在详情页隐藏，返回后恢复订单首页和底部导航。
- 交互验证：390px 宽度下验证首页不再包含订单详情区；点击订单进入详情页并显示对应订单号和状态；返回后回到订单页，待验收订单也能进入详情页；页面无横向溢出。

## 2026-09-14 管理端订单操作按履约状态流转

- 任务：区分待报价、待派单、施工中、待验收订单的操作按钮，并让管理端订单流程可连续演示。
- 主要实现：订单卡分别显示“去报价 / 去派单 / 跟进施工 / 查看验收”；详情页按状态切换为报价表单、派单设置、施工进度和验收进度；提交报价、确认派单、完工发起验收会依次推进订单状态，并同步更新顶部状态数字和筛选列表。
- 设计决策：管理端负责推进至待验收；客户通过后完成订单，客户不通过后进入管理端底部“验收”页处理复核，避免管理员代替客户执行验收。
- 交互验证：在移动端容器中确认四类订单卡按钮分别为“去报价 / 去派单 / 跟进施工 / 查看验收”；将 `JD20260914001` 从待报价连续推进到待派单、施工中、待验收，详情内容和按钮逐段切换正确，顶部数字从 `2/1/1/1` 更新为 `1/1/1/2`，返回列表后显示在待验收分类中。

## 2026-09-14 派单改为按确认等级选择具体师傅

- 任务：派单时不再填写师傅等级和人数，而是按客户已确认的金、银、铜人数，从对应分类中多选具体师傅姓名。
- 根因：管理端订单模型将客户端的 `gold / silver / bronze` 三类人数压扁成了单一 `grade + count`，导致无法保留客户对不同等级人数的确认结果，也无法落实到具体施工人员。
- 主要实现：订单保留三类确认人数；列表和详情均展示确认结果；待派单详情按有需求的等级展示师傅姓名复选项，达到该等级人数后限制继续选择；保存草稿允许未选满，确认派单时要求每个等级的姓名数量精确匹配确认人数；施工中和待验收展示最终执行师傅姓名。
- 交互验证：使用“金牌大师 2 位”订单验证未选择时确认派单会提示补齐；选择 1 人仍不满足确认数量；选满 2 人后第三人自动禁用；取消后第三人恢复可选；改选“陈师傅、刘师傅”并确认派单后，订单进入施工中且执行师傅显示两位具体姓名。移动端布局无按钮或文字溢出。

## 2026-09-15 双端订单逻辑对齐与闭环检查

- 任务：检查用户端和管理端的订单字段、状态、按钮权限与履约流程是否一致，并补齐报价确认和验收返修闭环。
- 根因：管理端原型提交报价后直接进入待派单，越过客户确认；用户端缺少报价确认入口；双端示例订单号和状态不一致；客户验收不通过后管理端仅提示成功，没有把订单送回施工。
- 主要实现：用户端增加待确认报价、上门地址及验收完成后的列表/历史更新；管理端增加“待客户确认”子状态、报价只读确认态、工期和地址详情；统一双端待验收订单数据；验收问题可安排返修并回到施工中，返修完成后再次进入待验收。
- 设计决策：顶部四个管理状态保持“待报价 / 待派单 / 施工中 / 待验收”；“待客户确认”归入待报价总类，客户确认前不开放派单；验收不通过单独进入问题队列，管理员安排后才转为返修中。
- 交互验证：验证报价提交不会提前进入待派单、客户确认报价、按等级精确选择师傅、施工完工、客户验收通过、不通过必填校验、管理端安排返修和返修后再次验收；双端控制台无错误；使用 390px 宽隔离视口确认用户端和管理端无横向溢出。审计截图和完整记录保存在 `audit/2026-09-15-two-sided-flow/`。
- 未决事项：静态 HTML 不包含后端、数据库、真实上传与跨端实时同步；正式小程序需按本次确认的订单状态机接入服务端。

## 2026-09-15 管理端派单增加师傅电话确认

- 任务：选择施工师傅时，需要提供按钮给师傅拨打电话确认。
- 主要实现：管理端派单列表中每个候选师傅旁新增“拨打”按钮；维护演示师傅电话映射；点击后触发 `tel:` 拨号并显示当前师傅号码；施工中“联系师傅”改为展示已派师傅及号码。
- 设计决策：电话确认只作为管理端派单辅助动作，不改变客户已确认的金/银/铜人数约束，也不改变“确认派单”前必须选满对应等级师傅的校验。
- 交互验证：验证管理端脚本语法、根目录与 `outputs/admin.html` 同步、390px 隔离视口下派单选择区无横向溢出；通过浏览器确认“电话”按钮出现且不会影响师傅勾选。

## 2026-09-15 管理端派单师傅按钮减拥挤

- 任务：派单选择区太挤，需要一行仅显示 2 个按钮：一个师傅选择、一个拨打电话；两类按钮要明显区分；每行前面显示序号；师傅名称使用完整姓名。
- 主要实现：候选师傅列表由双列改为单列；每行固定为“序号 + 师傅选择按钮 + 拨打按钮”；拨打按钮使用浅金色样式与选择按钮区分；候选池、已派订单、验收问题和师傅管理列表统一改为完整姓名。
- 交互验证：待派单详情在 390px 宽度下无横向溢出；序号、完整姓名和拨打按钮显示完整；师傅选择仍可正常勾选。

## 2026-09-16 新增师傅端小程序原型

- 任务：补齐师傅端，支持设置自己一周内上午/下午是否有空、查看被分配订单、预约客户、到达打卡、完工上传图片/视频、查看结算到账明细；管理端派单时显示订单要求施工时段下师傅的状态。
- 主要实现：新增 `master.html` 和 `outputs/master.html`，底部 Tab 为“时间 / 订单 / 结算 / 我的”；时间页可将每个半天切换为未知、有空、无空；订单页展示分配给李建军的订单并提供拨打客户、已预约、到达打卡、完工上传和提交完工；结算页展示订单金额、平台服务费、师傅到账、已到账/待到账汇总；管理端派单行新增可用状态标记。
- 设计决策：可用时间非必填，默认未知；管理端只显示对应订单上门日期和上午/下午的状态，不强制禁止选择“无空”师傅，保留电话确认和人工判断空间。
- 交互验证：检查 `admin.html`、`master.html` 脚本语法和重复 ID；同步根目录与 `outputs/`；在 390px 宽度下验证管理端派单状态标记和师傅端时间切换、订单操作、完工上传、结算显示均无横向溢出和控制台错误。

## 2026-09-16 管理端师傅资料和结算管理

- 任务：管理端“师傅”页面需要查看/修改师傅电话和介绍信息，并查看被分配、进行中、完成订单列表及需要支付、已支付金额。
- 主要实现：师傅页新增可点击师傅列表、资料编辑表单、需要支付/已支付金额汇总和按“被分配 / 进行中 / 完成”分组的订单列表；保存资料会同步列表电话和派单电话映射。
- 设计决策：在管理端原型内保留单页小程序形态，不新增二级页面；金额记录独立于订单状态示例，便于表达已完成订单的付款状态和待支付金额。
- 交互验证：检查 `admin.html`、`master.html` 脚本语法和重复 ID；同步 `admin.html` 到 `outputs/admin.html`；390px 宽度下验证师傅列表切换、电话/介绍保存、李建军金额汇总和三类订单分组，无横向溢出。

## 2026-09-16 GitHub 与 Vercel 发布

- 任务：将客户端、管理端、师傅端静态原型上传到 Vercel，并新建 GitHub 项目 `master`。
- 主要实现：将 `outputs/` 作为独立静态站点仓库根目录，包含 `index.html`、`admin.html`、`master.html`、`assets/jindashi-logo.png` 和 `README.md`；创建并推送 GitHub 仓库 `zzhaozi58/master`；通过 Vercel CLI 部署生产环境。
- 设计决策：只发布 `outputs/` 内可访问原型文件，不发布审计截图、开发日志和本地工作文件；提交 `.gitignore` 排除 Vercel 本地项目配置。
- 交互验证：GitHub 远程仓库创建并推送成功；Vercel 生产部署 READY；通过 `curl -I` 验证 `/`、`/admin.html`、`/master.html` 均返回 HTTP 200。
- 未决事项：Vercel 自动绑定 GitHub 仓库失败，原因是当前 Vercel 账号还需要添加 GitHub Login Connection；本次部署仍已成功上线，后续若要每次 GitHub push 自动部署，需要在 Vercel 账号连接 GitHub 后再绑定仓库。

## 2026-09-17 师傅等级分类、信用分与派单推荐 PRD

- 任务：管理端师傅管理补齐金/银/铜等级分类，增加管理员可手动调整的信用分，并将未来推荐排序逻辑写入 PRD。
- 主要实现：管理端师傅页新增“全部 / 金牌 / 银牌 / 铜牌”分类筛选；师傅列表扩展为 9 位师傅并展示信用分；师傅详情新增信用分输入并支持保存；派单候选排序改为先按订单时段有空状态，再按公平分配次数，再按信用分排序；新增 `PRD.md` 并同步到 `outputs/PRD.md`。
- 设计决策：信用分范围为 0-100，由管理员手动维护；推荐排序只改变候选展示顺序，不自动派单，管理员仍需电话确认并手动选择。
- 交互验证：检查 `admin.html`、`master.html` 脚本语法和重复 ID；同步 `admin.html` 与 `outputs/admin.html`、`PRD.md` 与 `outputs/PRD.md`；390px 宽度下验证全部 9 人、金牌筛选 3 人、信用分保存和派单排序，无横向溢出。
- 未决事项：本次仅本地更新原型与 PRD，未推送 GitHub 或重新部署 Vercel。

## 2026-09-17 师傅资料编辑补齐等级维护

- 任务：师傅资料编辑里缺失金/银/铜师傅等级选择。
- 主要实现：管理端师傅详情表单新增“师傅等级”下拉；保存资料时同步更新师傅等级、分类筛选结果和派单候选池；PRD 明确等级可由管理员调整。
- 设计决策：等级调整会立即影响后续派单候选范围，但不自动修改历史订单已选择师傅，避免改变已有订单记录。
- 交互验证：检查管理端脚本语法、重复 ID、根目录与 `outputs/` 同步；390px 宽度下验证等级选择、保存、筛选和派单候选联动。
- 未决事项：本次仅本地更新原型与 PRD，未推送 GitHub 或重新部署 Vercel。

## 2026-09-17 订单金额口径补齐

- 任务：管理端订单信息缺失与客户商定的订单金额；师傅端订单也需要显示金额，但应为扣除管理费后的实际到手金额。
- 主要实现：管理端订单列表、订单详情和各流程摘要新增“订单金额”；已确认/施工中/待验收订单使用客户商定金额，待报价订单显示待确认；师傅端订单卡新增“订单金额（实际到手）”，结算明细展示客户商定金额、平台管理费、师傅到账三项。
- 设计决策：同名“订单金额”在不同端保持业务语境一致：管理端面向客户合同金额，师傅端面向师傅收入金额；结算页再展开扣费明细以避免歧义。
- 交互验证：检查 `admin.html`、`master.html` 脚本语法和重复 ID；同步根目录与 `outputs/`；390px 宽度下验证管理端订单列表/详情金额和师傅端订单/结算金额显示。
- 未决事项：本次仅本地更新原型与 PRD，未推送 GitHub 或重新部署 Vercel。

## 2026-09-17 管理端已验收订单与手动款项确认

- 任务：管理端订单工作台缺失已验收订单；已验收订单需要区分收款或付款未完成，以及收付款已全部完成；款项完成必须由管理员手动点击。
- 主要实现：订单顶部状态新增“已验收”；已验收订单列表展示款项状态；详情页展示客户收款、师傅付款明细，并提供“确认已从客户收款”和“确认已支付给师傅”两个独立按钮；两项均完成后才显示“收付款已全部完成”。
- 设计决策：客户验收通过只代表履约验收完成，不自动代表客户已付款或平台已向师傅付款；收款和付款作为两个独立财务动作，由管理员人工确认。
- 交互验证：检查管理端脚本语法和重复 ID；同步 `admin.html` 与 `outputs/admin.html`、`PRD.md` 与 `outputs/PRD.md`；390px 宽度下验证已验收入口、两类款项状态和两个手动确认按钮。
- 未决事项：本次仅本地更新原型与 PRD，未推送 GitHub 或重新部署 Vercel。

## 2026-09-17 管理端客户资料可点击编辑

- 任务：管理端客户列表应该可以点击进入并编辑客户资料。
- 主要实现：客户页列表改为数据渲染；点击客户卡片后在详情表单中维护公司/用户名称、联系人、客户类型、微信名、电话、常用地址和备注；保存后同步刷新客户列表和详情。
- 设计决策：保持管理端单页小程序形态，不新增独立路由；客户编辑区与列表同屏，便于客服快速切换和维护 B 端资料。
- 交互验证：检查管理端脚本语法和重复 ID；同步 `admin.html` 与 `outputs/admin.html`、`PRD.md` 与 `outputs/PRD.md`；390px 宽度下验证客户卡片点击、资料保存和列表同步更新。
- 未决事项：本次仅本地更新原型与 PRD，未推送 GitHub 或重新部署 Vercel。

## 2026-09-18 管理端师傅详情改为二级页面

- 任务：师傅管理里点击师傅卡片，应该进入新页面查看及编辑。
- 主要实现：将师傅详情表单、支付汇总和订单分组从师傅列表页移入独立 `masterDetail` 页面；点击师傅卡片进入详情页；详情页提供返回师傅列表按钮；保存资料后同步更新列表、详情和派单候选池。
- 设计决策：沿用管理端单页小程序结构，用二级页面隐藏底部导航，和订单详情页保持一致的导航体验。
- 交互验证：检查管理端脚本语法和重复 ID；同步 `admin.html` 与 `outputs/admin.html`、`PRD.md` 与 `outputs/PRD.md`；390px 宽度下验证师傅卡片进入详情、返回、保存和列表同步。
- 未决事项：本次仅本地更新原型与 PRD，未推送 GitHub 或重新部署 Vercel。

## 2026-09-18 师傅详情订单卡片进入订单详情

- 任务：师傅详情里的订单卡片都需要进入新页面查看详情。
- 主要实现：师傅详情中的被分配、进行中、完成订单卡片增加点击入口；点击后进入现有订单详情页；从师傅详情进入订单详情时，返回按钮回到师傅详情；只有结算记录而不在当前订单池里的历史订单会生成可查看的详情快照。
- 设计决策：复用现有 `orderDetail` 二级页面，避免再新增一套订单详情 UI；返回目标按来源记录，保证从订单列表和师傅详情进入时路径都自然。
- 交互验证：检查管理端脚本语法和重复 ID；同步 `admin.html` 与 `outputs/admin.html`、`PRD.md` 与 `outputs/PRD.md`；390px 宽度下验证师傅详情订单卡片进入详情、返回师傅详情和历史订单快照。
- 未决事项：本次仅本地更新原型与 PRD，未推送 GitHub 或重新部署 Vercel。

## 2026-09-18 客户端验收 Tab 改为订单管理

- 任务：客户端“验收”Tab 应该是“订单”，用于管理所有订单，尤其是未报价、施工中、未验收订单。
- 主要实现：底部 Tab 文案改为“订单”；原验收页改为“我的订单”页；订单页展示等待报价、待确认报价、施工中、待验收、已完成等订单；待确认报价可在订单页确认，待验收订单才显示验收通过/不通过和问题提交区。
- 设计决策：保留原有验收交互作为订单页的条件操作区，避免把验收从订单生命周期里割裂出来；“我的”页回归资料维护、订单历史和客服入口。
- 交互验证：检查 `index.html` 脚本语法和重复 ID；同步 `index.html` 与 `outputs/index.html`、`PRD.md` 与 `outputs/PRD.md`；390px 宽度下验证订单 Tab、未报价、施工中、待验收、报价确认和验收操作。
- 未决事项：本次仅本地更新原型与 PRD，未推送 GitHub 或重新部署 Vercel。

## 2026-09-19 客户端订单按状态分类

- 任务：待验收订单应统一为一类，不再按瓷砖缺角、家具补漆、大理石裂缝等修补类型拆分顶部 Tab。
- 主要实现：订单页顶部改为 `待报价 / 待确认报价 / 待派单 / 施工中 / 待验收 / 已完成` 状态 Tab，并显示各状态订单数；分类下用订单卡片列表展示具体订单，点击卡片后查看详情和执行对应操作。
- 设计决策：修补材质和类型保留在订单标题与详情中，仅用于识别订单，不作为订单导航维度；验收通过或不通过后，订单自动迁移到对应状态分类并更新数量。
- 交互验证：脚本语法及重复 ID 检查通过；Playwright 在 390x844 下确认仅有一个待验收状态 Tab、待验收分类列出 3 笔订单、切换订单后验收对象同步、验收通过后订单由待验收迁移至已完成；页面宽度 390px，无横向溢出。
- 未决事项：本次仅本地更新原型与 PRD，未推送 GitHub 或重新部署 Vercel。

## 2026-09-19 客户端分类内直接展示订单详情

- 任务：客户端订单页保留状态分类 Tab，但分类内不再同时显示订单标签和单个详情；已完成订单除外，以简略条目进入独立详情页。
- 主要实现：待报价、待确认报价、待派单、施工中、待验收分类直接渲染该分类全部完整详情卡片；待验收卡片保留独立验收入口；已完成分类展示简略条目并新增客户端订单详情二级页面。
- 设计决策：客户端详情仅展示维修、服务师傅、客户订单金额、完工、验收、客户付款和完工凭证；不复用管理端内部处理字段，也不暴露师傅端管理费及实际到手金额。
- 交互验证：脚本语法和重复 ID 检查通过；Playwright 在 390x844 下验证状态 Tab、待验收分类 3 张完整卡片、已完成简略条目、客户端详情页、返回路径和底部导航隐藏；页面无横向溢出。
- 未决事项：本次仅本地更新原型与 PRD，未推送 GitHub 或重新部署 Vercel。

## 2026-09-19 伤口数量与长度输入框对齐

- 任务：伤口总数与伤口总长度两个输入框宽度、高度保持一致。
- 主要实现：仅为该字段组增加等宽双列布局、统一标签区域高度，并固定两个数字输入框为 46px 高；不影响上门时间与预计工期字段。
- 验证：静态检查确认专用样式只作用于 `damage-fields`，HTML 脚本解析和重复 ID 检查通过。

## 2026-09-19 客户端待验收订单进入独立验收页

- 任务：点击待验收订单的“验收此订单”后进入新页面完成验收。
- 主要实现：新增客户端 `clientAcceptance` 二级页面，展示当前订单的维修内容、上门时间、服务师傅、客户订单金额和施工进度；验收通过、不通过、问题图片/视频和问题说明表单均移动到该页面；返回按钮回到订单列表。
- 设计决策：订单列表只保留验收入口，不在列表页下方展开验收表单；验收通过或提交返修问题后返回订单列表，并按结果更新订单分类。
- 验证：HTML 脚本解析、重复 ID、根目录与 `outputs/index.html` 同步校验通过；本次未启动浏览器自动化。

## 2026-09-19 客户端待派单演示订单

- 任务：客户端“待派单”分类没有订单，增加 2 笔演示订单。
- 主要实现：新增岩板缺角修复和木门补漆修复两笔待派单订单，分别展示金牌大师 2 位、银牌大师 1 位加铜牌大师 1 位的客户需求、预约时间、客户订单金额和派单状态。
- 验证：HTML 脚本解析、重复 ID 和 `outputs/index.html` 同步校验通过；本次未启动浏览器自动化。

## 2026-09-19 客户端验收页展示完工凭证

- 任务：待验收订单的验收页面需要查看师傅上传的图片、视频和完工文字说明。
- 主要实现：新增“师傅完工凭证”区域，提供完工图片、完工视频两个预览入口和师傅完工说明文本；三笔待验收 Demo 订单分别维护自己的媒体数量与文字说明，进入不同订单时同步更新。
- 验证：HTML 脚本解析、重复 ID 和 `outputs/index.html` 同步校验通过；本次未启动浏览器自动化。

## 2026-09-20 师傅注册资料与管理端师傅信息

- 任务：增加师傅端注册 / 完善资料页面，并让管理端师傅资料覆盖姓名、性别、微信号、手机号、常住地址、个人介绍和信用分。
- 主要实现：师傅端从“我的”进入独立资料页，支持必填校验、编辑回填、保存后回写资料摘要；管理端师傅详情新增资料字段，列表同步展示关键联系信息，保存后同步等级资源池、派单候选、订单引用、结算记录和可用时间键。
- 设计决策：信用分仍由管理端维护；师傅注册资料不包含等级和信用分，避免师傅端越权修改管理字段；当前原型仅在内存中模拟保存，不涉及真实注册、短信验证或后端持久化。
- 验证：`admin.html`、`master.html` 及 `outputs/` 对应副本脚本解析、重复 ID 检查通过；根目录与 `outputs/` 文件同步通过；本次未启动浏览器自动化。

## 2026-09-20 新注册师傅默认铜牌

- 任务：明确新注册师傅的初始等级。
- 主要实现：师傅注册页展示“注册初始等级：铜牌大师”，提交反馈和 PRD 均明确新注册默认归入铜牌；金银等级继续由管理端维护。

## 2026-09-20 师傅常住地址拆分

- 任务：常住地址拆分为城市、区/县、街道三个手动输入框。
- 主要实现：师傅端注册页三项均设为必填并参与校验；管理端师傅资料使用相同三个字段编辑和展示，资料摘要按三段拼接。

## 2026-09-20 管理端师傅资料只读化

- 任务：管理端师傅资料暂时仅保留等级和信用分编辑能力。
- 主要实现：姓名、性别、微信号、联系电话、城市、区/县、街道和个人介绍改为文本只读；等级保留下拉编辑，信用分保留数字编辑，保存逻辑仅更新等级和信用分。

## 2026-09-20 师傅资料展示改为文本行

- 任务：只读师傅资料不再使用灰色输入框样式。
- 主要实现：管理端师傅详情改为左侧字段名、右侧文本值的资料行展示；等级保留下拉，信用分保留行内数字编辑。

## 2026-09-20 师傅等级和信用分位置调整

- 任务：管理端师傅详情中，可编辑的师傅等级和信用分放在个人介绍后面。
- 主要实现：保持姓名、性别、联系方式、地址和个人介绍为文本展示；将等级下拉框与信用分数字输入框移动到个人介绍之后，保存逻辑不变。

## 2026-09-20 师傅已支付金额可编辑

- 任务：管理端师傅详情中的已支付金额也需要使用输入框并可编辑。
- 主要实现：将金额区纳入师傅资料保存表单；需要支付保持只读汇总，已支付金额改为数字输入框；结算记录增加按已付金额汇总和部分支付状态，保存后同步师傅列表、详情和订单结算卡片。
- 验证：本次使用静态脚本解析、重复 ID、字段存在性和根目录/`outputs` 同步检查；未启动浏览器自动化。

## 2026-09-20 管理端订单展示师傅金额

- 任务：管理端所有订单卡片和订单详情在订单金额下方增加师傅金额。
- 主要实现：新增统一的师傅金额计算函数，按订单金额 90% 计算；订单列表卡片、订单详情基础信息和各状态流程摘要均在订单金额下一行展示师傅金额，未确认金额显示待确认。
- 验证：静态脚本解析、重复 ID、字段存在性和根目录/`outputs` 同步检查通过；未启动浏览器自动化。

## 2026-09-20 师傅端结算金额收敛

- 任务：师傅端结算页的金额只显示应收金额和实收金额。
- 主要实现：结算页总览改为本月应收金额和实收金额；订单结算明细仅展示每单应收金额、实收金额；移除师傅端结算页中的客户商定金额、平台管理费和待到账金额展示。
- 验证：静态脚本解析、重复 ID、结算页禁用字段检查和根目录/`outputs` 同步检查通过；未启动浏览器自动化。

## 2026-09-20 师傅端订单分类合并结算

- 任务：师傅端改为类似管理端的订单分类，只保留与自己相关的订单类型，并取消独立结算 Tab。
- 主要实现：底部导航改为“时间 / 订单 / 我的”；订单页新增待预约、施工中、待验收、已完成分类 Tab；历史结算记录合并为已完成订单卡片；每张相关订单卡片内展示应收金额和实收金额。
- 验证：静态脚本解析、重复 ID、结算 Tab 移除、订单分类字段存在性和根目录/`outputs` 同步检查通过；未启动浏览器自动化。

## 2026-09-21 师傅端订单金额字段统一

- 任务：师傅端各订单 Tab 的金额字段重复且口径不一致，需要将“订单金额（实际到手）”统一改为“应收金额”。
- 主要实现：移除订单卡片中的“订单金额（实际到手）”；所有订单状态统一按“上门时间 / 预计工期 / 应收金额 / 地址 / 备注 / 到场打卡”的顺序展示，仅已完成订单追加“实收金额”，未打卡订单显示“未打卡”。
- 验证：静态脚本解析、重复 ID、金额字段与四个订单分类标记检查通过；未启动浏览器自动化。

## 2026-09-21 基于三端原型完善正式开发 PRD

- 任务：根据客户端、管理端和师傅端现有原型，补齐页面中不可见的内部业务逻辑，形成正式开发前的产品基线。
- 主要实现：重构 `PRD.md`，新增三端角色权限、核心数据模型、统一订单状态机与三端映射、报价版本、派单约束与推荐排序、返修轮次、金额和多师傅分配、跨端同步、通知、审计、隐私、非功能要求及 MVP 验收标准；明确手动派单、手动收付款和师傅端金额口径。
- 重要决策：正式系统以单一后端订单为事实来源；管理员待报价包含等待客户确认子状态；自动化只做候选排序；履约和款项状态独立；每次返修保留独立完工与验收轮次；未在原型中定案的登录、定位、媒体限制和异常流程列为开发前待确认项。
- 验证：对照三个 HTML 的页面、字段、状态、操作与金额逻辑进行静态检查，并检查 PRD 章节、状态映射、关键规则和 `outputs/PRD.md` 同步；未启动浏览器自动化。

## 2026-09-21 PRD 1.1 确认决策与异常流程

- 任务：将用户对 PRD 待确认问题的回复纳入正式业务规则，并将管理端底部“验收” Tab 改为“异常”。
- 主要更新：明确三个独立 AppID 共用后端、管理员白名单授权、师傅注册审核、待施工分类、客户取消与已取消终态、异常处理与强制完成、定位失败仍可打卡、完工图片和视频数量、多师傅完工后手工分配金额，以及客户和师傅手机号不做短信验证。
- 产品决策：管理端“异常”统一处理取消申请和验收不通过；正常待验收订单仍在订单工作台的“待验收”分类跟进。最新 PRD 的业务规则高于尚未补齐的静态原型。
- 验证：仅进行 PRD 关键术语、状态映射、原型差异说明和根目录 / `outputs` 副本一致性检查；未修改 HTML，未启动浏览器或 UI 自动化。

## 2026-09-21 三端小程序工程第一版

- 任务：根据 PRD 开始正式开发客户端、管理端、师傅端三个微信小程序端，并优先打通共享业务状态机。
- 主要实现：新增 `miniprograms/client`、`miniprograms/admin`、`miniprograms/master` 三个小程序工程目录，各自包含 `app.json`、`app.js`、全局样式、Tab 页面和关键二级页面；新增 `shared/domain.js` 作为本地 Mock 后端和统一订单状态机，覆盖询价、报价、客户确认、派单、预约、无定位打卡、完工、验收、异常、取消、返修、强制完成、师傅审核、金额分配及收付款确认。
- 三端页面：客户端包含询价、订单、独立验收、我的；管理端包含订单工作台、订单详情、异常、师傅、师傅详情、客户；师傅端包含可用时间、订单、注册 / 完善资料、我的。
- 技术决策：三个小程序端运行时各自使用端内 `utils/domain.js`，根目录 `shared/domain.js` 作为源文件；`scripts/check-shared-sync.js` 用于检查三端副本是否与源文件一致。当前仍为本地 Mock 数据层，后续需要替换为真实后端 API、对象存储、登录鉴权和微信能力。
- 验证：`npm run verify` 通过，覆盖 8 条业务状态机测试及三端共享层同步检查；所有 `shared`、`tests`、`scripts`、`miniprograms` 下 JS 文件 `node -c` 语法检查通过；所有小程序 JSON 配置和 `package.json` 解析通过。未启动浏览器、微信开发者工具或 UI 自动化。

## 2026-09-21 后端服务边界与小程序静态兼容检查

- 任务：继续推进三端小程序正式开发，补齐 PRD 要求的三端共用后端事实源和角色权限边界，并减少微信 WXML 编译兼容风险。
- 主要实现：新增 `backend/service.js`，在本地内存层模拟正式后端服务，提供客户、师傅、管理员三类角色 API；客户只能操作自己的订单，师傅只能查看已分配且审核通过后的订单，管理员入口需要 `admin_` 白名单形态并负责报价、派单、异常、审核、收付款和审计。新增 `tests/backend.test.js` 覆盖角色权限、跨端共享状态推进、待审核师傅限制和敏感操作审计。
- 小程序修复：客户端询价页移除 WXML 动态下标和字符串拼接，改由 JS 生成上传提示、修复类型选中态和金 / 银 / 铜人数；管理端派单候选序号改由 JS 生成；订单金额、师傅金额、应收 / 实收金额展示文本改由共享业务层生成。
- 验证增强：新增 `scripts/check-miniprogram-static.js`，检查 WXML 中的复杂动态下标 / 字符串拼接表达式及页面同名 JS/JSON/WXSS 配套文件；`npm run verify` 现覆盖 8 条领域状态机测试、4 条后端服务测试、共享层同步检查和 14 个 WXML 页面静态检查。未启动浏览器、微信开发者工具或 UI 自动化。

## 2026-09-21 三端页面切换到 API 抽象层

- 任务：减少页面与本地状态机的直接耦合，为后续接真实后端 `wx.request` 做准备。
- 主要实现：新增 `miniprograms/client/utils/api.js`、`miniprograms/admin/utils/api.js`、`miniprograms/master/utils/api.js`；客户端、管理端、师傅端页面均改为调用 `app.globalData.api`，不再直接调用 `globalData.domain` 或 `globalData.state`。端内 API 方法名与 `backend/service.js` 的角色 API 保持一致，方便下一步替换传输层。
- 管理端调整：订单详情通过 API 获取订单视图和候选师傅；师傅、客户、异常、收付款等操作均走管理 API。共享订单视图补充 `requestedMasters`，用于派单页按金 / 银 / 铜人数生成候选组。
- 验证：新增 `tests/miniprogram-api.test.js` 覆盖客户端询价、管理端报价派单、师傅端保存时间 / 预约 / 打卡 / 完工；`scripts/check-miniprogram-static.js` 增加页面层不得使用 `globalData.domain/state` 的检查。`npm run verify` 通过，现包含 8 条领域测试、4 条后端服务测试、3 条端内 API 测试、共享层同步和 14 个 WXML 静态检查。

## 2026-09-21 本地 HTTP 后端与 JSON 持久化

- 任务：把本地内存服务继续推进为可通过真实 HTTP 请求调用的后端边界，并支持重启后状态恢复。
- 主要实现：新增 `backend/http-server.js`、`backend/storage.js`、`backend/server.js` 和 `backend/README.md`；HTTP 服务按客户、师傅、管理员三类接口暴露订单、报价、派单、验收、异常、师傅审核、客户资料和收付款能力；JSON 存储使用临时文件原子替换写入，默认数据文件为 `data/jindashi-state.json`。
- 接口补齐：客户可维护个人资料；管理员可查看和编辑客户详情；管理接口保留 `admin_` 形态白名单校验。正式微信登录和服务端鉴权后续仍需替换当前演示身份参数。
- 验证：新增 `tests/http-server.test.js`，通过真实 HTTP 请求推进“管理员报价 -> 客户确认 -> 管理员派单 -> 师傅预约/打卡/完工 -> 客户验收”主流程，验证状态写入 JSON 后重启仍可读；同时覆盖客户资料维护持久化、客户越权和管理员未授权拒绝。`npm run verify` 通过，现包含 8 条领域测试、5 条后端服务测试、3 条端内 API 测试、3 条 HTTP 集成测试、共享层同步和 14 个 WXML 静态检查。

## 2026-09-21 小程序远程 HTTP API 传输层

- 任务：让三端小程序 API 层可从本地内存模式切换到真实 HTTP 请求模式，为接入本地后端和后续生产后端做准备。
- 主要实现：三端新增 `utils/request.js` 和 `utils/config.js`；`utils/api.js` 保留本地实现并新增 `createRemoteClientApi`、`createRemoteAdminApi`、`createRemoteMasterApi`。`app.js` 根据 `backendBaseUrl` 自动选择本地内存 API 或远程 HTTP API。所有页面调用改为 `async/await`，避免远程模式下把 Promise 当作数据使用。
- 配置说明：`backendBaseUrl` 为空时使用端内本地数据；设置为 `http://localhost:8787` 时通过 `wx.request`/`fetch` 访问本地 HTTP 后端。真机调试需要改为局域网地址并配置合法请求域名。
- 验证：新增 `tests/remote-miniprogram-api.test.js`，通过三端远程 API 连接真实 HTTP 服务，覆盖报价、确认、派单、预约、打卡、完工、验收、资料维护和取消异常。`scripts/check-miniprogram-static.js` 增加页面层调用 `app.globalData.api` 必须 `await` 或 `return` 的检查。`npm run verify` 通过，现包含 8 条领域测试、5 条后端服务测试、3 条端内 API 测试、3 条 HTTP 集成测试、2 条远程小程序 API 集成测试、共享层同步和 14 个 WXML 静态检查。

## 2026-09-21 本地微信身份登录与 token 鉴权

- 任务：把 PRD 中“服务端使用 openid/unionid 和管理员白名单授权”的规则落到本地后端边界，减少对前端传业务 ID 的依赖。
- 主要实现：`shared/domain.js` 增加本地 Demo 微信身份映射和 session 列表；新增 `backend/auth.js`，提供 `/auth/login` 的模拟微信登录、token 签发和 `Authorization: Bearer` 鉴权。客户、师傅、管理接口优先从 token 解析业务主体和角色；旧的 `customerId/masterId/adminId` 参数保留为开发兼容路径。
- 三端远程 API：`createRemoteClientApi`、`createRemoteAdminApi`、`createRemoteMasterApi` 支持 `login()` 和 `authToken`；配置 token 后请求通过 Authorization 头鉴权，不再发送业务 ID。
- 重要边界：当前 `/auth/login` 仍是本地模拟，测试中直接传 `appid/openid/unionid`。正式上线时前端必须传 `wx.login` 的 `code`，服务端用微信接口换取 `openid/unionid` 后再签发平台 token。
- 验证：`tests/http-server.test.js` 增加 token 登录与角色错配拒绝；`tests/remote-miniprogram-api.test.js` 增加三端远程 API 使用 token 推进流程。`npm run verify` 通过，现包含 8 条领域测试、5 条后端服务测试、3 条端内 API 测试、4 条 HTTP 集成测试、3 条远程小程序 API 集成测试、共享层同步和 14 个 WXML 静态检查。

## 2026-09-21 本地媒体上传与订单凭证接入

- 任务：继续从静态原型推进正式小程序开发，补齐客户询价、师傅完工、客户验收问题所需的图片 / 视频上传链路。
- 主要实现：新增 `backend/media-store.js`，本地后端提供 `POST /media/upload`、`GET /media/:id` 和元数据读取；上传文件保存到 `data/media`，媒体元数据写入共享状态 `mediaFiles`。三端远程 API 新增 `uploadMedia()`，客户端询价、验收问题、师傅完工页面优先使用 `wx.chooseMedia` 读取 base64 后上传，测试 / 本地模式保留同结构演示媒体。
- 重要边界：当前媒体上传是本地 JSON/base64 模拟，用于验证业务链路和数据结构；生产上线仍需替换为私有对象存储或云开发文件能力，并补服务端文件类型、大小、访问权限和临时 URL 策略。
- 验证：新增 `tests/media-upload.test.js`，覆盖真实字节上传、媒体读取、媒体元数据持久化，以及媒体对象进入客户询价和师傅完工凭证。

## 2026-09-21 师傅到场打卡定位记录

- 任务：落实 PRD 中“到达打卡必须先调用微信定位能力，保存定位成功或失败结果”的业务规则。
- 主要实现：`Assignment` 增加定位结果字段，保存 `locationOk`、经纬度、精度和失败原因；师傅端订单页打卡前调用 `wx.getLocation`，定位失败时仍允许打卡并把失败原因传给后端；订单展示沿用统一的打卡文本。
- 重要边界：MVP 不因为定位失败阻断打卡，但必须记录失败原因；生产环境需要在小程序后台配置定位权限说明，并由服务端记录真实请求来源和时间。
- 验证：领域测试新增定位成功与失败分支；HTTP 和远程小程序 API 流程改为覆盖定位成功提交。

## 2026-09-21 报价草稿、版本历史与旧报价作废

- 任务：落实 PRD 中“保存草稿不通知客户、管理员修改已提交报价时旧报价作废、客户只能确认最新有效报价”的规则。
- 主要实现：订单新增 `quoteHistory`，继续保留 `quote` 作为当前报价；管理端新增保存草稿入口和 `/quote-draft` 接口，草稿只进入报价历史且不改变订单状态；再次提交报价时，旧的待客户确认报价标记为“已作废”，新报价按历史最大版本号递增；客户确认时只确认当前待确认报价，并记录确认时间。管理端订单详情展示报价版本、状态和金额。
- 重要边界：草稿当前只保存历史记录，不做多草稿编辑合并；后续如需要“继续编辑上次草稿”，可在管理端取最后一个草稿回填。
- 验证：领域测试新增报价草稿不改变状态、两次提交报价后旧版本作废、最新版确认并锁定订单金额；端内 API、HTTP 和远程小程序 API 流程均覆盖保存草稿入口。

## 2026-09-21 通知事件队列与失败重试边界

- 任务：落实 PRD 中关键操作建议触发通知，且消息发送失败不得回滚核心业务状态、必须记录失败并允许重试的规则。
- 主要实现：共享状态新增 `notificationOutbox`；报价提交、客户确认报价、派单、预约、打卡、完工、验收通过 / 不通过、返修、强制完成、取消、师傅付款确认和师傅注册审核等关键动作会写入通知事件。管理端服务和 HTTP API 新增通知列表、标记失败和重新加入重试队列能力，三端远程管理 API 同步支持。
- 重要边界：当前只实现本地通知队列和状态管理，不真正发送微信订阅消息；生产接入时应由独立发送器消费 `notificationOutbox`，并保留发送结果、模板 ID 和微信返回错误码。
- 验证：领域测试覆盖草稿不通知、关键状态变更入队、失败与重试；后端、HTTP 和远程小程序 API 测试覆盖通知失败记录和重试入口。

## 2026-09-21 款项明细记录与重复确认幂等

- 任务：落实 PRD 中“履约状态与款项状态独立保存，汇总金额必须由明细计算，重复提交不得生成重复记录”的规则。
- 主要实现：共享状态新增 `paymentRecords` 和 `paymentAdjustments`；客户验收通过或管理员强制完成后生成客户收款与师傅付款明细；多师傅金额分配会为每位师傅生成 / 更新应付记录，金额调整保留前后值；确认客户收款和确认师傅付款更新明细记录，并保留旧的汇总字段兼容现有页面。管理端订单详情展示款项明细，后端和远程管理 API 增加 `listPaymentRecords()`。
- 重要边界：当前为人工确认全额收款 / 付款，不支持部分收付款输入；后续如需要账期或分次付款，应扩展 `paymentRecords` 的多笔流水模型。
- 验证：领域测试覆盖验收后生成款项明细、多师傅金额分配、重复确认幂等和师傅付款通知不重复；后端、HTTP 和远程小程序 API 测试覆盖款项明细读取。

## 2026-09-21 审计日志前后值增强

- 任务：落实 PRD 中关键操作审计需要记录操作者、时间、前后值和对象 ID 的要求。
- 主要实现：审计记录兼容原有 `action/objectId/detail`，新增 `before`、`after` 和 `actor` 字段；师傅等级 / 信用分 / 已支付金额调整、客户资料修改、客户自助资料修改、师傅资料修改、可用时间修改、金额分配和收付款确认会记录前后值。
- 重要边界：当前审计写入本地状态数组，生产应落库并防篡改；更细粒度管理员角色权限仍待后续实现。
- 验证：领域测试覆盖师傅管理字段修改的前后值；后端测试覆盖管理员修改客户资料的前后值。

## 2026-09-21 客户资料导出审计

- 任务：落实 PRD 中客户数据导出属于管理权限操作，必须控制权限并记录导出日志的要求。
- 主要实现：管理端后端服务与 HTTP API 新增 `exportCustomers()` / `/admin/customers-export`，返回导出时间、数量和客户记录；导出动作写入审计日志，记录导出对象与数量。远程管理 API 同步支持。
- 重要边界：当前返回 JSON 结构化数据，不生成 CSV/XLSX 文件；正式后台如需文件下载，应在导出任务里生成文件并记录文件 ID、下载人和下载时间。
- 验证：后端、HTTP 和远程小程序 API 测试覆盖客户资料导出与审计记录。

## 2026-09-21 管理端订单搜索与分页

- 任务：落实 PRD 中管理端订单工作台支持状态筛选、关键词搜索和列表分页 / 增量加载的要求。
- 主要实现：共享层新增 `queryAdminOrders()`，支持按状态、关键词、页码和页大小查询，并返回总数、是否还有更多以及各状态数量；后端和远程管理 API 新增 `queryOrders()`。管理端订单页增加搜索框、总数显示和“加载更多”，保留原有状态按钮计数。
- 重要边界：老的 `listOrders()` 和不带分页参数的 HTTP `/admin/orders` 仍返回数组，兼容已有调用；带 `keyword/page/pageSize` 时返回分页对象。
- 验证：领域、端内 API、HTTP 和远程小程序 API 测试覆盖关键词搜索、分页和状态数量。

## 2026-09-21 HTTP 接口幂等键

- 任务：落实 PRD 中订单、报价、派单、验收和支付接口必须支持幂等的要求，降低重复点击或网络重试导致重复记录的风险。
- 主要实现：共享状态新增 `idempotencyRecords`；HTTP 层支持 `Idempotency-Key` header 或 body `idempotencyKey`。同一操作者、同一路径、同一 key 的重复 `POST` 请求会返回第一次结果，不再重复调用业务状态机。远程管理 API 的报价、草稿和派单可透传 `idempotencyKey`。
- 重要边界：当前为本地持久化幂等记录，未做过期清理；生产应使用服务端生成/校验的幂等存储、TTL 和并发锁。
- 验证：HTTP 和远程小程序 API 测试覆盖重复提交报价不生成重复报价版本。

## 2026-09-21 师傅可用时间窗口校验

- 任务：落实 PRD 中师傅端维护未来连续 7 天、每天上午 / 下午可用状态，未填写显示未知的规则。
- 主要实现：共享层新增 `getMasterAvailability()`，统一生成未来 7 天共 14 个时段并合并已保存状态；`saveAvailability()` 校验日期必须在窗口内、时段必须为上午 / 下午、状态必须为未知 / 有空 / 无空。师傅端页面改为从 API 读取窗口，后端新增 `GET /master/availability`。
- 重要边界：当前日期基于本地固定 `nowText()`，生产应使用服务端当前时间和 `Asia/Shanghai` 时区滚动生成。
- 验证：领域、端内 API、HTTP 和远程小程序 API 测试覆盖默认未知、保存窗口和非法日期拒绝。

## 2026-09-21 师傅审核重提与取消订单操作保护

- 任务：补齐师傅注册资料被拒绝后的重新提交闭环，并避免已取消订单在师傅端仍触发拨打电话、预约、打卡或完工提交。
- 主要实现：师傅资料更新时，如果当前审核状态为“已拒绝”，修改资料后自动回到“待审核”，清空拒绝原因，且审核通过前仍不可查看订单；师傅订单页已取消订单的按钮事件改为显式识别取消状态后直接返回。
- 重要边界：重新提交只改变审核状态，不自动通过审核；管理端仍需再次审核后师傅才进入候选池和订单列表。
- 验证：后端服务测试和师傅端本地 API 测试新增拒绝后重提断言；`npm run verify` 通过。

## 2026-09-22 师傅累计付款明细冲抵与取消保留

- 任务：继续按 PRD 收敛管理端师傅结算和异常处理逻辑，避免汇总金额脱离结算明细，并补齐客户取消异常的“保留订单”分支。
- 主要实现：管理端师傅列表 / 详情改为从已验收订单的师傅付款记录汇总“应付、已付、需付”；管理员编辑累计已支付金额时，按该师傅已验收订单从早到晚冲抵付款记录、同步订单分配记录并写入调整日志，且禁止超过应付总额。异常状态机新增“管理员保留订单”，客户取消申请可被驳回并恢复发起取消前状态，同时保留异常处理记录、审计和通知。
- 重要边界：累计已支付金额只能分配到已有已验收且已生成应付的师傅结算记录；没有结算记录的师傅不能凭空写入已支付金额。
- 验证：领域测试覆盖累计付款冲抵、超额拒绝和保留订单；后端服务测试覆盖管理端保留取消申请；`npm run verify` 通过。

## 2026-09-22 多人订单预约与完工后金额分配

- 任务：补齐 PRD 中多人订单每位师傅独立预约 / 打卡记录，以及管理端完工后手工分配每位师傅应收金额的页面能力。
- 主要实现：多人订单在第一位师傅确认预约后，其他被派师傅仍可继续确认自己的预约；师傅金额分配被收紧为必须在完工后执行。管理端订单详情新增“分配师傅金额”区域，列出每位执行师傅和金额输入框，保存时调用统一 API 并校验合计等于师傅金额总池；未完工时只展示禁用状态和提示。
- 重要边界：多人订单派单阶段只确定执行师傅，不提前拆分金额；金额未分配完成时仍不能确认师傅付款。
- 验证：领域测试覆盖多人分别预约、完工前禁止分配、合计校验和付款确认；端内管理 API 测试覆盖完工后分配师傅金额；`npm run verify` 通过。

## 2026-09-22 客户端已完成订单详情与资料校验

- 任务：补齐 PRD 中客户端已完成订单独立详情页，并把“我的信息”必填和联系电话格式校验落到共享规则。
- 主要实现：客户端新增 `pages/order-detail/order-detail`，已完成订单和我的订单历史可点击进入，展示订单号、维修内容、上门时间、服务师傅、客户订单金额、完工时间、验收结果、客户付款状态和完工凭证，不暴露师傅金额、平台管理费或内部备注。客户资料保存增加前端提示，并在共享领域层提供 `validateCustomerProfile()`，端内 API 和后端客户接口复用校验。
- 重要边界：客户端订单详情只展示客户可见信息；订单历史继续读取订单页同一数据源，不维护静态副本。
- 验证：端内 API 测试覆盖客户资料必填和电话格式；后端服务测试覆盖无效电话拒绝；`npm run verify` 通过，静态检查识别客户端新增页面。

## 2026-09-22 师傅注册校验与管理端客户详情页

- 任务：补齐 PRD 中师傅注册 / 完善资料的服务端校验，并把管理端客户列表改成点击进入独立详情维护。
- 主要实现：共享领域层新增 `validateMasterProfile()`，师傅姓名、性别、微信号、手机号、城市、区县和街道必填，手机号格式必须有效；师傅端页面、端内 API 和后端师傅接口都复用或兜底校验。管理端新增 `pages/customer-detail/customer-detail`，客户列表卡片点击进入详情页编辑，列表不再同页展开表单；管理员更新客户资料也复用客户资料校验。
- 重要边界：师傅个人介绍仍为选填；客户备注仍为管理端内部字段，不进入客户端详情。
- 验证：后端和端内 API 测试覆盖师傅无效手机号拒绝、管理员客户资料无效电话拒绝；`npm run verify` 通过，静态检查识别 16 个 WXML 页面。

## 2026-09-22 师傅审核拒绝原因闭环

- 任务：落实 PRD 中“拒绝注册必须填写原因，师傅端显示审核未通过并允许修改资料重新提交”的规则。
- 主要实现：共享状态机在拒绝师傅注册时强制要求原因；管理端师傅详情在待审核状态展示拒绝原因输入框，拒绝操作使用管理员填写内容，不再硬编码固定原因。师傅端订单页区分待审核和已拒绝状态，已拒绝时展示拒绝原因并提供修改资料入口。
- 重要边界：重新提交资料只回到待审核，不自动通过；师傅仍需管理员再次审核后才可查看订单。
- 验证：领域测试覆盖无原因拒绝失败和拒绝原因保存；后端测试覆盖无原因拒绝失败、原因可见、修改资料后清空原因并回到待审核；`npm run verify` 通过。

## 2026-09-22 异常处理说明与处理时间

- 任务：落实 PRD 中客户取消、保留订单、驳回验收异常并强制完成等敏感异常处理必须填写处理说明并保留处理记录的要求。
- 主要实现：管理端异常页为待处理异常增加“处理说明”输入；确认取消、保留订单和驳回异常强制完成均使用管理员输入内容，不再硬编码说明。异常记录补齐 `adminNote` 和 `handledAt`，安排返修、确认取消、保留订单、强制完成都会记录处理时间。
- 重要边界：安排返修本身不要求处理说明；强制完成和取消 / 保留仍由领域层强制要求说明。
- 验证：领域测试覆盖确认取消处理说明与时间、强制完成无说明失败、强制完成写入说明 / 时间 / 款项记录；后端服务测试覆盖强制完成说明必填和收付款记录生成；`npm run verify` 通过。

## 2026-09-22 媒体文件归属与读取权限

- 任务：落实 PRD 中上传媒体、文件归属和核心文件访问必须有后端权限校验的要求。
- 主要实现：`/media/upload` 和 `/media/:id` 接入角色身份，token 模式优先使用登录态，本地 Demo 兼容 `ownerRole + customerId/masterId/adminId`。媒体读取增加权限判断：管理员可读全部；上传者可读自己的媒体；客户可读自己订单关联媒体；师傅可读已分配订单关联媒体。三端远程 API 上传媒体时补传各自业务身份，避免空 `ownerId`。
- 重要边界：当前仍是本地文件存储和本地身份模拟；生产应替换为私有对象存储和短期授权 URL，但不能退回公开直链。
- 验证：媒体上传测试覆盖无身份读取失败、上传客户读取成功、派单后相关师傅读取成功、非订单客户读取失败；`npm run verify` 通过。

## 2026-09-22 联系客服入口与验收付款幂等覆盖

- 任务：补齐客户端“我的”页联系客服入口，并扩展 PRD 中验收和支付接口幂等的验证覆盖。
- 主要实现：客户端配置新增 `servicePhone`，App 注入全局客服电话；我的页“拨打客服”按钮调用 `wx.makePhoneCall`，非微信环境显示客服电话。静态检查新增“拨打客服”必须绑定 `callService` 的规则。HTTP 测试新增验收通过、确认客户收款、确认师傅付款的幂等键重复提交场景，确保不会生成重复记录。
- 重要边界：当前客服电话为本地 Demo 配置，正式上线需要替换为平台客服号码或客服会话入口。
- 验证：`npm run verify` 通过，新增静态检查覆盖客服按钮，HTTP 测试覆盖验收与付款接口幂等。

## 2026-09-22 师傅完工文字说明输入

- 任务：补齐 PRD 中师傅完工时可填写文字说明，并在客户验收页展示的链路，避免师傅端提交固定文案。
- 主要实现：师傅端施工中订单增加“完工文字说明（选填）”输入；提交完工时使用师傅实际填写内容，成功后清空该订单输入状态。静态检查新增“提交完工”必须存在完工说明输入绑定，防止页面回退为仅上传媒体。
- 重要边界：文字说明仍为选填；完工图片和视频数量规则继续由共享领域层校验。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 管理端师傅等级固定选择

- 任务：落实管理端师傅资料中金 / 银 / 铜等级必须用固定选项维护，避免自由输入产生无效等级。
- 主要实现：师傅详情页“师傅等级”由输入框改为三档 picker；保存仍走 `updateMasterAdminFields()`，继续由共享领域层校验等级合法性。静态检查新增管理端师傅详情等级必须使用固定 picker 的约束。
- 重要边界：师傅姓名、电话、微信号、常住地址和个人介绍仍为只读文本；当前可编辑项保持为等级、信用分和已支付金额。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 客户端期望上门时间选择器

- 任务：把首页询价表单中的期望上门时间从普通文本输入改为小程序原生日期 / 时间选择，贴近正式交互。
- 主要实现：客户端询价页新增日期 picker 和时间 picker，选择后组合为原有 `visitTime` 字段，继续供提交询价、候选师傅时段匹配和后台展示使用。静态检查新增“期望上门时间”必须使用日期 picker 的约束。
- 重要边界：后端字段格式仍为 `YYYY-MM-DD HH:mm`；当前不限制可选日期范围，正式上线可按业务需要增加最早 / 最晚日期。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 上传反馈与重试入口

- 任务：落实 PRD 中上传操作需要展示进度、失败原因和重试入口的要求。
- 主要实现：客户端询价现场媒体、客户验收问题凭证增加“上传中 / 上传完成 / 上传失败”提示，失败时展示重试上传按钮；师傅端提交完工前提示正在上传完工凭证，失败时展示具体失败原因并允许重新点击提交。
- 重要边界：当前为页面级轻量反馈，不做单文件级进度条、断点续传或后台队列；媒体访问权限和数量规则仍由后端 / 共享领域层校验。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 客户视图隐藏报价草稿

- 任务：修正客户订单视图可能带出管理端报价草稿历史的问题，落实“报价草稿只留管理端记录”的权限边界。
- 主要实现：共享 `toClientOrder()` 过滤状态为“草稿”的报价历史，客户侧只拿到非草稿报价版本；客户可见报价和最新完工轮次返回副本，避免前端持有共享状态引用。同步更新三端内置 `domain.js` 副本。
- 重要边界：管理端仍可查看完整报价历史，包括草稿；客户确认报价仍只能确认当前有效的待确认报价。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 派单等级后端校验

- 任务：修复派单状态机只校验金 / 银 / 铜人数、未校验所选师傅真实等级的问题。
- 主要实现：`dispatchOrder()` 在校验各等级人数时，同时逐个校验对应数组中的师傅等级必须匹配金牌 / 银牌 / 铜牌要求；新增领域测试覆盖把金牌师傅塞入铜牌名额会被拒绝。同步更新三端内置 `domain.js` 副本。
- 重要边界：前端候选列表仍负责提供按等级筛选和拨打电话确认的操作体验；后端状态机作为最终约束，防止绕过 UI 造成错派。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 师傅本人打卡后才能完工

- 任务：修复多人订单中只要任一师傅打卡进入施工中，其他未打卡师傅也可能绕过本人打卡提交完工的问题。
- 主要实现：`submitCompletion()` 增加当前师傅本人 `checkedInAt` 校验；师傅订单视图增加 `canCheckIn` 和 `canComplete`，页面上仅本人已打卡后才允许提交完工。新增领域测试覆盖多人订单未打卡师傅提交完工会被拒绝。同步更新三端内置 `domain.js` 副本。
- 重要边界：定位失败仍视为已完成打卡，会记录失败原因并允许后续完工；取消订单仍保持按钮禁用和水印。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 派单候选公平分补齐结算记录

- 任务：落实 PRD 中候选排序的公平分必须同时考虑已分配订单和结算记录，不能只按已分配订单数排序。
- 主要实现：`rankCandidates()` 增加 `settlementCount` 和 `fairnessScore`，排序改为可用状态、`fairnessScore`、信用分、姓名。新增领域测试覆盖信用分更高但已有结算记录的师傅不会排在公平分更低的师傅前面。同步更新三端内置 `domain.js` 副本。
- 重要边界：公平分只影响推荐排序，不自动选择师傅；管理员仍需电话确认并手动派单。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 关键操作审计前后值和操作者

- 任务：落实 PRD 中报价、派单、打卡、完工、验收、异常、收付款和敏感管理操作必须记录操作者、时间、前后值与对象 ID 的要求。
- 主要实现：共享状态机为关键状态流转和金额操作补充 `before / after` 快照；后端客户、师傅和管理员入口向状态机传入实际业务操作者 ID。新增后端测试覆盖主流程中的客户确认报价、管理员派单、师傅打卡、师傅完工和客户验收均有非 system 操作者及前后值。同步更新三端内置 `domain.js` 副本。
- 重要边界：本地小程序内存模式未经过后端角色包装时仍使用默认 `system`，正式后端路径已记录真实业务 ID；生产还应把审计日志落库并做防篡改策略。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。普通沙箱下 HTTP 测试因不能监听 `127.0.0.1` 失败，已按权限要求提升后重跑通过。

## 2026-09-22 管理端订单详情补齐客户媒体和确认人数

- 任务：补齐 PRD 中管理端订单详情必须展示客户上传图片 / 视频以及客户确认的师傅等级人数的要求。
- 主要实现：管理端订单视图数据增加客户提交媒体副本；订单详情页展示客户图片 / 视频数量和金银铜确认人数。静态检查新增管理端订单详情必须包含这两个字段的约束。同步更新三端内置 `domain.js` 副本。
- 重要边界：当前小程序原型展示媒体数量，不做逐个媒体预览；媒体读取权限仍由后端 `/media/:id` 校验。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 订单客户信息快照

- 任务：落实 PRD 中客户资料更新后，历史订单必须保留提交时客户信息快照的规则。
- 主要实现：订单实体增加 `customerSnapshot`；初始化 Demo 订单和客户新提交询价都会写入提交时的客户快照。客户端和管理端订单视图读取订单快照而不是实时客户档案，避免客户后续改名、改电话、改微信号影响历史订单。新增后端测试覆盖客户提交订单后修改资料，客户端和管理端仍看到订单提交时的信息。同步更新三端内置 `domain.js` 副本。
- 重要边界：客户档案仍会被资料维护更新；快照仅用于历史订单展示和追溯。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 客户端已完成订单摘要卡片

- 任务：落实 PRD 中客户端“已完成”分类只展示简略条目，点击后进入独立订单详情的要求。
- 主要实现：客户端订单页在已完成分类下隐藏上门时间和服务师傅等完整详情字段，仅保留订单号、订单金额、付款状态、进度摘要和查看详情入口。静态检查新增已完成订单卡片必须保持摘要形态的约束。
- 重要边界：未完成分类仍直接展示完整详情卡片；已完成订单的完整信息继续在独立 `order-detail` 页面展示。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 客户端报价疑问入口

- 任务：补齐 PRD 中待确认报价时“对报价有疑问”应打开平台沟通入口且不改变订单状态的交互。
- 主要实现：客户端订单页待确认报价卡片新增“对报价有疑问”按钮，复用全局客服电话配置调用 `wx.makePhoneCall`；非微信环境展示客服电话。静态检查新增报价疑问文案必须绑定 `callService` 的约束。
- 重要边界：点击该入口只用于沟通，不改变报价、订单状态或审计链路；正式上线可替换为微信客服会话能力。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 管理端师傅详情订单分组与结算汇总

- 任务：补齐 PRD 中师傅详情需查看被分配、进行中、完成订单列表，以及需要支付 / 已支付金额的要求。
- 主要实现：管理端师傅详情增加只读结算汇总行；相关订单按“被分配订单 / 进行中订单 / 完成订单”分组展示，并保留订单卡片点击进入订单详情。静态检查新增师傅详情必须包含订单分组和需要支付字段的约束。
- 重要边界：已支付金额仍作为可编辑管理字段保留在下方输入框，保存后继续由结算明细冲抵逻辑处理。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 完工凭证展示与师傅端媒体数量前置校验

- 任务：补齐客户验收页必须查看师傅完工图片 / 视频和文字说明，以及师傅端完工媒体数量规则应在提交前明确校验的要求。
- 主要实现：客户验收页在图片 / 视频数量下方列出本轮完工凭证条目，保留完工文字说明；师傅端提交完工时先校验 1-5 张图片和 1-2 个视频，再上传和提交；管理端师傅列表增加专长 / 介绍摘要，便于列表层筛选判断。静态检查新增验收页必须渲染完工图片和视频列表的约束。
- 重要边界：当前小程序原型列出凭证名称 / 标识，不直接拉取私有媒体预览；正式预览仍由后端媒体权限接口控制。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 派单设置草稿持久化

- 任务：落实 PRD 中“保存派单设置允许暂未选满”的要求，避免管理端页面刷新后丢失已电话确认的部分师傅选择。
- 主要实现：订单实体新增 `dispatchDraft`；共享领域层提供 `saveDispatchDraft()`，允许少选但校验不能超选、错等级、重复选择或选择未审核师傅。管理端订单详情新增“保存派单设置”按钮，本地 API、远程 API、后端服务和 HTTP 路由全部接入；确认派单后自动清空草稿。PRD 同步更新派单草稿边界，并删除多人金额分配控件的过期说明。
- 重要边界：保存草稿不改变订单状态、不通知客户或师傅；只有“确认派单”才创建师傅分配记录并推动订单进入待施工。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 管理端待验收提醒客户

- 任务：补齐 PRD 中管理端待验收订单需要联系客户和发送验收提醒的跟进动作。
- 主要实现：共享领域层新增 `remindAcceptance()`，仅待验收订单可生成“提醒客户验收”通知并记录审计；管理端本地 API、远程 API、后端服务和 HTTP 路由接入；订单详情待验收状态显示“发送验收提醒”按钮。静态检查新增提醒按钮必须绑定处理函数。
- 重要边界：发送提醒不改变订单状态，也不替代客户本人通过或不通过验收；通知发送失败仍走既有失败记录和重试队列。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 订单详情媒体条目展示

- 任务：补齐 PRD 中订单详情需展示客户上传图片 / 视频、客户已完成详情需展示完工凭证的页面证据，避免只显示数量。
- 主要实现：客户端已完成订单详情在完工图片 / 视频数量下方列出本轮凭证条目；管理端订单详情在客户图片 / 视频数量下方列出客户上传条目。静态检查新增两个详情页必须渲染媒体条目列表的约束。
- 重要边界：当前小程序原型展示媒体名称 / 标识；实际文件预览仍应经由后端私有媒体访问接口控制。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 状态变更按钮提交期禁用

- 任务：落实 PRD 中状态变更按钮提交期间禁用、避免重复操作的页面层要求。
- 主要实现：客户端询价提交、报价确认、取消申请、验收通过 / 不通过、资料保存；管理端报价、派单、金额分配、验收提醒、收付款、异常处理、师傅审核 / 管理字段保存、客户资料保存；师傅端预约、打卡、完工提交、注册资料提交、可用时间保存均增加页面级 `submitting` 守卫和禁用态样式。
- 重要边界：后端幂等仍是最终防线；页面禁用用于改善交互并降低重复点击请求。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 多师傅金额调整历史补强

- 任务：补强 PRD 中“多人金额后续修改必须保留调整历史”的审计证据。
- 主要实现：多人订单重新分配师傅应收金额时，`paymentAdjustments` 记录补充操作者和来源说明；领域测试覆盖金额二次调整后留下 before / after、actor 和 source。
- 重要边界：调整历史仍为本地状态机记录；正式落库时需要与审计日志一起持久化和防篡改。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 金额分字段补强

- 任务：落实 PRD 中“所有金额由后端使用整数分存储，前端仅格式化展示”的内部金额精度要求。
- 主要实现：报价、订单金额、收付款明细和付款汇总在保留现有元字段展示兼容的同时，新增 `repairFeeCents`、`visitFeeCents`、`totalCents`、`orderAmountCents`、`dueAmountCents`、`paidAmountCents` 等整数分字段；确认报价、生成款项明细、确认客户收款和确认师傅付款时同步维护分字段。领域测试覆盖报价确认和收付款明细的分字段。
- 重要边界：当前仍保留元字段供小程序原型页面展示；正式落库时应以分字段作为金额权威字段，元字段仅作为派生展示值。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 收付款确认人补强

- 任务：落实 PRD 中“客户收款和师傅付款确认必须记录管理员、时间和金额”的款项明细要求。
- 主要实现：`PaymentRecord` 新增 `confirmedBy`、`confirmedAmount`、`confirmedAmountCents`；确认客户收款、确认师傅付款和管理员编辑累计已支付金额时同步维护确认人、确认金额和确认时间。管理端订单详情的款项明细展示确认人、确认金额和确认时间。领域测试和后端角色测试覆盖管理端确认后可从款项明细读到确认人和确认金额。
- 重要边界：本期仍为人工全额确认按钮；部分付款只通过管理员编辑师傅累计已支付金额模拟冲抵，后续若支持分次收付款需扩展为多笔流水。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 师傅审核信息补强

- 任务：落实 PRD 中师傅资料必须保存审核管理员、审核时间和审核备注的要求。
- 主要实现：师傅实体新增 `reviewedBy`、`reviewedAt`、`reviewNote`；管理员通过或拒绝注册时同步记录审核人、时间和备注，拒绝原因同时作为审核备注；被拒绝师傅修改资料重新提交时清空上一轮审核信息并回到待审核。管理端师傅详情展示审核管理员、审核时间和审核备注，静态检查新增字段保护。
- 重要边界：审核通过备注目前为空或沿用审核调用传入的备注；正式运营若需要结构化审核意见，可在管理端通过 / 拒绝弹层扩展备注输入。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-22 款项记录备注补强

- 任务：落实 PRD 中 `PaymentRecord` 必须包含备注，并在收付款确认中保留备注的要求。
- 主要实现：款项明细新增 `note` 字段；确认客户收款和确认师傅付款支持传入备注，未传时写入默认人工确认备注；HTTP 管理端接口和三端管理 API 透传备注。管理端订单详情款项明细展示备注，静态检查新增备注展示保护。测试覆盖领域层默认 / 自定义备注和 HTTP 幂等付款备注透传。
- 重要边界：当前页面仍使用两个确认按钮，不额外要求管理员输入备注；正式运营若需要更精细的财务备注，可在确认前增加弹层输入。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-23 订单状态时间戳补强

- 任务：落实 PRD 中订单需要保存创建、更新及各状态时间戳的要求。
- 主要实现：订单实体新增 `statusTimes`，创建订单时记录初始状态时间；报价确认、派单、预约、打卡施工、完工待验收、验收通过 / 不通过、返修、取消、保留订单恢复状态等主状态流转统一通过 `setOrderStatus()` 记录进入时间。管理端订单数据返回 `statusTimes`，领域测试覆盖报价到待派单以及预约、施工、待验收、已验收的时间戳。
- 重要边界：当前原型使用固定 `nowText()` 方便测试；正式后端应以服务器时间和 `Asia/Shanghai` 展示规则生成真实时间。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-23 异常记录操作者补强

- 任务：落实 PRD 中异常记录必须保留发起人、处理管理员、处理说明和处理时间的要求。
- 主要实现：客户取消和验收不通过异常新增 `createdBy`；确认取消、保留订单、安排返修、驳回异常并强制完成时写入 `handledBy`、`handledAt` 和处理说明。管理端异常页展示发起人和处理人，本地三端 API 补充客户 / 管理员操作者传递，静态检查新增异常操作者展示保护。领域测试和后端测试覆盖发起人、处理人写入。
- 重要边界：当前异常发起人 / 处理人保存业务 ID；正式上线可在展示层映射为姓名或角色名称，但审计底层仍应保留稳定 ID。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-23 派单时间与备注补强

- 任务：落实 PRD 中 `Assignment` 需要派单时间和派单备注的要求。
- 主要实现：师傅分配记录新增 `dispatchedAt` 和 `dispatchNote`；管理员确认派单时写入派单时间和默认 / 自定义备注，HTTP 后端与管理端远程 API 支持传入派单备注。师傅端订单备注优先展示派单备注，便于师傅查看电话确认后的施工说明。领域、HTTP 和本地小程序 API 测试覆盖派单备注写入和师傅端可见。
- 重要边界：当前管理端页面尚未提供派单备注输入框，未传备注时写入默认“管理员电话确认后派单”；后续可在派单区增加备注输入。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-23 报价提交人补强

- 任务：落实 PRD 中 `Quote` 需要记录提交人的要求。
- 主要实现：报价对象新增 `submittedBy`；正式提交报价和保存报价草稿时写入管理员 ID，本地管理端 API 使用 `admin_root`，后端 / 远程 API 使用当前管理员身份。领域、本地管理端 API、后端服务和远程 API 测试覆盖提交人写入。
- 重要边界：历史种子报价未显式传入时默认 `system`；正式数据迁移可按审计日志补齐历史提交人。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-23 报价订单号补强

- 任务：落实 PRD 中 `Quote` 需要记录订单号的要求，避免报价记录脱离订单上下文。
- 主要实现：报价对象新增 `orderId`，报价 ID 改为包含订单号和版本号；种子报价、正式提交报价、报价草稿均写入订单号，并对旧形态报价在订单归一化时回填 `orderId`。同步更新三端小程序共享 domain 副本。
- 重要边界：该改动不改变报价金额、状态流转和客户确认逻辑，仅补强数据可追溯性。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-23 验收轮次追溯补强

- 任务：落实 PRD 中完工 / 验收轮次与异常记录需要保留订单号、客户提交时间、管理员处理方式 / 时间和验收凭证的要求。
- 主要实现：`AcceptanceCycle` 新增 `orderId`、`customerSubmittedAt`、`adminAction`、`adminHandledAt`；客户通过验收或不通过验收时写入提交时间，管理员安排返修或强制完成时写入处理方式和处理时间。验收不通过异常新增 `evidence` 保存客户问题图片 / 视频，并在管理端异常页展示凭证数量。同步更新三端小程序共享 domain 副本。
- 重要边界：历史轮次在订单归一化时回填新字段，不覆盖已有完工凭证、客户问题或管理员处理结果。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-23 派单记录订单号补强

- 任务：落实 PRD 中 `Assignment` 需要记录订单号的要求，让每条“订单 + 师傅”分配记录可独立追溯。
- 主要实现：分配记录新增 `orderId`；种子订单和正式派单创建分配记录时写入订单号，历史分配记录在订单归一化时回填。师傅端订单响应新增只读 `assignment` 快照，管理端和师傅端都能拿到同一条分配记录。同步更新三端小程序共享 domain 副本。
- 重要边界：该改动不改变派单人数校验、状态流转、金额计算或候选排序。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。

## 2026-09-23 师傅可用时间更新时间补强

- 任务：落实 PRD 中 `MasterAvailability` 需要记录更新时间的要求。
- 主要实现：种子可用时间与师傅保存可用时间时写入 `updatedAt`；读取未来 7 天窗口时返回已保存记录的更新时间，未填写的未知时段更新时间为空。同步更新三端小程序共享 domain 副本。
- 重要边界：该改动不改变未来 7 天、上午 / 下午、未知 / 有空 / 无空的校验和排序规则。
- 验证：`npm run verify` 通过；小程序 JS 语法和 JSON 配置解析检查通过。
