# 金大师补漆维修微信小程序 PRD

版本：1.1  
更新时间：2026-09-22  
文档状态：基于客户端、管理端、师傅端高保真原型整理，作为正式开发前的产品基线

## 1. 文档目的

本 PRD 将当前三个静态原型中的页面、字段和交互，整理为可实施的业务规则。正式开发必须以本 PRD 的状态流转、权限、金额口径和跨端同步规则为准，不能仅按页面视觉逐页实现。

当前原型用于确认产品流程，不具备真实后端、数据库、对象存储、消息通知、支付、登录鉴权或跨端实时同步。正式开发时，三个端必须共享同一套订单、客户、师傅、报价、验收和结算数据。

## 2. 产品定位与目标

金大师补漆维修面向家具厂、石材厂、建材门店、家居品牌、物业工程部和售后服务团队，提供从报修、定损、报价、派单、施工、验收到收付款记录的维修履约服务。

核心目标：

- 客户能够清楚提交损伤信息并追踪订单状态。
- 管理员能够统一报价、匹配师傅、处理返修和记录款项。
- 师傅能够维护可用时间、完成预约与打卡、提交完工凭证并查看自己的应收与实收金额。
- 每次报价、派单、施工、验收、返修和款项确认均可追溯。

MVP 的派单方式为“系统排序推荐 + 管理员电话确认 + 管理员手动派单”，不是无人值守自动派单。客户端原型中的“自动派单、拒单或超时自动顺延”属于后续能力，正式 MVP 文案应改为与本规则一致。

## 3. 产品端与角色

### 3.1 客户端

面向企业客户和个人客户，主要功能：

- 查看参考价格、服务方式和保障。
- 提交维修需求并获取报价。
- 确认报价或提出报价疑问。
- 查看派单、施工、返修、验收和付款状态。
- 查看师傅完工图片、视频和文字说明。
- 通过验收，或提交不通过问题及凭证。
- 维护客户资料、查看订单历史、联系客服。

客户只能查看和操作自己账号下的订单，不得查看平台内部师傅金额、信用分、派单排序、其他客户资料或内部处理备注。

### 3.2 管理端

面向平台管理员和客服，主要功能：

- 查看全部客户订单并按状态处理。
- 定损、填写报价、保存草稿、提交报价和提醒客户确认。
- 根据客户确认的师傅等级与人数选择具体师傅。
- 查看师傅可用时间、分配量和信用分排序，并拨打电话确认。
- 跟进施工，并在“异常”模块处理客户取消、验收不通过、返修和强制完成。
- 手动确认客户收款及师傅付款。
- 查看和维护客户档案。
- 查看师傅资料；维护师傅等级、信用分和已支付金额。

### 3.3 师傅端

面向认证维修师傅，主要功能：

- 注册或完善个人资料，并等待管理员审核通过。
- 维护未来一周上午、下午的可用状态。
- 查看仅分配给自己的订单。
- 拨打客户电话、确认预约、到场打卡。
- 上传完工图片、视频和文字说明。
- 查看每单应收金额；仅在已完成订单查看实收金额。

师傅不得查看客户支付总额、平台管理费、其他师傅的金额、其他师傅订单、管理员信用分调整记录或平台内部备注。

## 4. MVP 导航结构

### 4.1 客户端

底部 Tab：

- `询价`
- `订单`
- `我的`

二级页面：

- 订单验收
- 已完成订单详情

### 4.2 管理端

底部 Tab：

- `订单`
- `异常`
- `师傅`
- `客户`

二级页面：

- 订单详情
- 师傅详情

### 4.3 师傅端

底部 Tab：

- `时间`
- `订单`
- `我的`

二级页面：

- 注册 / 完善资料

师傅端不设置独立“结算”Tab，金额信息合并在订单中。

### 4.4 小程序与账号架构

正式版本采用三个独立小程序 AppID：客户端、师傅端、管理端。三端共用同一个后端、数据库和业务状态机，并绑定到同一个微信开放平台账号。

- 客户端面向公开客户，可独立发布和运营。
- 师傅端只面向注册并审核通过的师傅。
- 管理端只面向服务器白名单中的管理员，不因知道入口或转发链接而获得权限。
- 后端使用内部用户 ID 作为业务主键，优先用 `unionid` 关联三端身份，同时保存 `appid + openid` 映射。不同 AppID 的 `openid` 不得直接当作跨端统一用户 ID。

管理员身份不能通过手动填写的真实微信号自动识别。首位管理员第一次登录管理端后，由服务端取得该 AppID 下的 `openid` 以及可用时的 `unionid`，再通过服务器配置或数据库白名单授予管理员角色。用户提供的微信号只用于人工核对，不写入前端代码，也不作为登录凭证。

## 5. 核心数据模型

正式开发应至少包含以下业务实体。字段名可由技术方案调整，但语义必须保持一致。

### 5.1 客户 Customer

- 客户 ID
- 微信账号标识：`openid`，跨小程序场景需要时使用 `unionid`
- 公司名称 / 用户名字
- 联系人
- 客户类型
- 微信号 / 微信昵称：用户手动填写
- 联系电话
- 常用上门地址
- 客户备注：仅管理端可见
- 创建时间、更新时间

小程序不能直接读取用户真实微信号。微信号或昵称必须由用户手动填写，不能把微信昵称或 `openid` 当作真实微信号。

### 5.2 师傅 Master

- 师傅 ID 与微信账号标识
- 姓名
- 性别
- 微信号：手动填写
- 手机号
- 常住城市
- 常住区 / 县
- 常住街道及门牌
- 个人介绍
- 能力标签 / 擅长材料
- 师傅等级：金牌、银牌、铜牌
- 接单状态
- 信用分：0-100
- 注册状态：待审核 / 已通过 / 已拒绝 / 已停用
- 审核管理员、审核时间、审核备注
- 创建时间、更新时间

新注册师傅默认铜牌且状态为“待审核”。只有管理员审核通过后，师傅才能进入派单候选池和查看被分配订单。师傅本人可维护基础资料；管理端暂时只允许审核注册、修改等级、信用分和已支付金额，其他资料只读。

### 5.3 师傅可用时间 MasterAvailability

- 师傅 ID
- 日期
- 时段：上午 / 下午
- 状态：有空 / 无空 / 未知
- 更新时间

未填写即为“未知”。上午定义为 12:00 前，下午定义为 12:00 及以后。可用时间用于推荐排序，不代表平台已锁定该时段。

### 5.4 订单 Order

- 订单号：全局唯一
- 客户 ID
- 客户提交的图片、视频
- 修复材质及“其他”文本
- 修复类型列表及“其他”文本
- 伤口总数
- 伤口总长度（厘米）
- 期望上门时间
- 预计工期（天）
- 上门地址
- 客户期望的金 / 银 / 铜师傅人数
- 当前履约状态与子状态
- 取消状态、取消原因、取消申请人和取消时间
- 当前报价版本
- 客户确认的订单金额
- 师傅金额总池
- 具体执行师傅及每位师傅应收金额
- 派单备注、施工进度
- 打卡时间、定位状态、经纬度、定位精度和定位失败原因
- 完工轮次、完工凭证、验收轮次
- 客户应收、已收；师傅应付、已付
- 创建时间、更新时间及各状态时间戳

### 5.5 报价 Quote

- 报价 ID、订单号、版本号
- 维修报价
- 上门费
- 报价合计
- 报价说明
- 状态：草稿 / 待客户确认 / 已确认 / 已作废
- 提交人、提交时间、客户确认时间

管理员修改已提交报价时，旧报价必须作废并生成新版本。客户只能确认最新有效版本，防止客户确认已被管理员修改的旧报价。

### 5.6 派单 Assignment

- 订单号
- 师傅 ID
- 师傅等级快照
- 派单状态
- 师傅应收金额：单师傅订单可直接计算，多师傅订单在完工后由管理员分配
- 派单时间、预约时间、打卡时间
- 派单备注

一笔订单可对应多条师傅分配记录。

### 5.7 完工与验收轮次 AcceptanceCycle

每次完工和客户验收组成一个独立轮次，返修后必须创建新轮次，不得覆盖原记录。

- 轮次编号
- 订单号
- 师傅上传的完工图片、视频、文字说明
- 完工提交时间
- 客户结果：待确认 / 通过 / 不通过
- 客户问题图片、视频、文字说明
- 客户提交时间
- 管理员处理结果与处理时间
- 管理员处理方式：安排返修 / 驳回异常并强制完成

### 5.8 款项记录 PaymentRecord

- 订单号
- 类型：客户收款 / 师傅付款
- 对象 ID：客户或具体师傅
- 应收 / 应付金额
- 已收 / 已付金额
- 状态：未完成 / 部分完成 / 已完成
- 管理员确认人、确认时间、备注

履约状态与款项状态必须独立保存。客户验收通过不等于客户已付款，也不等于平台已向师傅付款。

### 5.9 异常记录 ExceptionRecord

- 异常 ID、订单号
- 异常类型：客户取消 / 验收不通过
- 发起人、发起时间、原因和凭证
- 发起时订单状态快照
- 处理状态：待处理 / 已取消 / 已安排返修 / 已驳回并强制完成
- 处理管理员、处理说明和处理时间

异常记录必须保留完整历史。处理完成后可以归档，但不得从订单审计链中删除。

## 6. 统一订单状态机

### 6.1 主流程

1. `待报价`：客户已提交需求，平台尚未提交报价。
2. `待客户确认报价`：管理员已提交报价，等待客户确认；管理端仍归入“待报价”大类。
3. `待派单`：客户已确认最新报价，订单金额和师傅等级人数已锁定。
4. `待预约`：管理员已选择具体师傅，师傅尚未确认与客户预约。
5. `已预约`：师傅已电话联系客户并确认预约，尚未到场。
6. `施工中`：师傅已到场打卡，或订单处于返修施工阶段。
7. `待验收`：师傅已提交完工凭证，等待客户验收。
8. `验收不通过待处理`：客户提交问题，等待管理员复核和安排返修。
9. `返修中`：管理员已安排返修，订单回到施工阶段。
10. `已验收`：客户通过验收，履约完成，进入收付款跟进。
11. `已取消`：管理员在允许取消的阶段确认取消，订单终止且不可继续履约。

管理端增加 `待施工` 分类，用于承接已经派单但尚未到场施工的订单，包含统一状态中的“待预约”和“已预约”。任一执行师傅完成到场打卡后，订单从管理端“待施工”进入“施工中”。

### 6.2 三端状态映射

| 统一状态 | 客户端分类 | 管理端分类 | 师傅端分类 |
|---|---|---|---|
| 待报价 | 待报价 | 待报价 | 不显示 |
| 待客户确认报价 | 待确认报价 | 待报价 | 不显示 |
| 待派单 | 待派单 | 待派单 | 不显示 |
| 待预约 | 施工中，显示待预约 | 待施工 | 待预约 |
| 已预约 | 施工中，显示已预约 | 待施工 | 施工中 |
| 施工中 | 施工中 | 施工中 | 施工中 |
| 验收不通过待处理 | 施工中，显示待返修 | 异常 | 待验收，显示等待异常处理 |
| 返修中 | 施工中 | 施工中 | 施工中 |
| 待验收 | 待验收 | 待验收 | 待验收 |
| 已验收 | 已完成 | 已验收 | 已完成 |
| 已取消 | 已完成，显示已取消 | 异常 | 已完成，显示已取消 |

### 6.3 状态触发规则

| 当前状态 | 操作人 | 操作及前置条件 | 新状态 |
|---|---|---|---|
| 无订单 | 客户 | 必填字段校验通过并提交需求 | 待报价 |
| 待报价 | 管理员 | 提交有效报价 | 待客户确认报价 |
| 待客户确认报价 | 客户 | 确认最新报价版本 | 待派单 |
| 待客户确认报价 | 管理员 | 修改报价 | 待报价，旧报价作废 |
| 待派单 | 管理员 | 各等级师傅选择数量与客户确认数量完全一致 | 待预约 |
| 待预约 | 师傅 | 已电话联系客户并点击“已预约” | 已预约 |
| 已预约 | 师傅 | 发起到场打卡；定位成功或定位失败都保存打卡结果 | 施工中 |
| 施工中 / 返修中 | 师傅 | 上传 1-5 张完工图片和 1-2 个完工视频并提交 | 待验收 |
| 待验收 | 客户 | 通过验收 | 已验收 |
| 待验收 | 客户 | 选择不通过，且凭证和说明校验通过 | 验收不通过待处理 |
| 验收不通过待处理 | 管理员 | 点击“安排返修” | 返修中 |
| 验收不通过待处理 | 管理员 | 填写处理说明并驳回异常 | 已验收，标记管理员强制完成 |
| 返修中 | 师傅 | 重新提交新一轮完工凭证 | 待验收 |
| 待报价 / 待客户确认报价 / 待派单 / 待预约 / 已预约 | 管理员 | 确认客户取消或管理员取消，并填写取消原因 | 已取消 |

所有状态变更必须由后端校验当前状态并原子更新，重复点击不得创建重复报价、重复派单、重复验收轮次或重复款项记录。

### 6.4 取消规则

- 客户取消可由客户端提交申请，或由管理员根据电话、微信沟通结果代为录入。
- 只有管理端分类为待报价、待派单、待施工的订单可以确认取消；已经到场进入施工中的订单不能使用本取消流程。
- 取消必须填写原因，并在管理端“异常”Tab 留存记录。
- 尚未派单的订单取消后，不产生师傅端记录。
- 已经派单但尚未施工的订单取消后，所有分配给师傅的订单卡片显示明显“已取消”水印；预约、打卡、完工上传、提交等按钮全部禁用并显示灰色。
- 已取消订单为终止状态，不自动生成客户收款或师傅付款。若线下已经发生款项，管理员通过人工财务备注处理，本期不包含自动退款。

## 7. 客户端需求

### 7.1 询价首页

首页顺序：品牌头部、参考价格表、服务方式与保障、维修需求表单。

#### 参考价格

以下价格为宣传单提供的常规参考价，不是系统自动报价结果：

| 材质范围 | 损伤类型 | 参考价 |
|---|---|---:|
| 实木 / 板材 / 科技木 / 木门 / 橱柜 / 楼梯 | 浅表划痕、烫痕、水印、漆面发白、钉眼 | 50 元/处 |
| 同上 | 深划痕、磕碰掉漆、缺角、凹坑、露底 | 120 元/处 |
| 同上 | 家具开裂、缝隙修补 | 150 元/处 |
| 瓷砖 / 岩板 / 石材 | 浅表划痕 | 100 元/处 |
| 同上 | 深划痕、崩瓷、缺角、小磕碰 | 130 元/处 |
| 同上 | 拼缝、开裂、断裂 | 170 元/处 |
| 大理石 | 浅表划痕 | 120 元/处 |
| 大理石 | 深划痕、崩瓷、缺角、小磕碰 | 150 元/处 |
| 大理石 | 拼缝、开裂、断裂 | 190 元/处 |

价格规则：

- 成都绕城内上门费 50 元/次。
- 点状损伤以 5cm x 5cm 为一处。
- 细长划痕、裂缝每 5cm 为一个计价单位。
- 超过 30cm、大面积掉漆、整面翻新、整块门板等按面积和施工难度现场报价。
- 复杂木纹、双面漆、珠光、金属漆、多色拼接等复杂工艺加收 30-100 元/处。
- 周边城市按距离面议；结构松动、断裂、承重、拆装加固等需现场报价。
- 普通维修不承诺颜色、纹理和光泽绝对一致，最终报价及施工边界以平台确认内容为准。

#### 维修需求字段与校验

| 字段 | 规则 |
|---|---|
| 图片 / 视频 | 必填，图片和视频合计至少一项，均支持多选 |
| 修复材质 | 必填单选：木材、实木、瓷砖、岩板、大理石、水磨石、其他 |
| 其他材质 | 仅选择“其他”时展开且必填；“其他”可取消选择 |
| 修复类型 | 必填多选：划痕、坑洞、缺角、裂缝、拼缝、补漆、其他 |
| 其他类型 | 仅选择“其他”时展开且必填；“其他”可取消选择 |
| 伤口总数 | 必填整数，最小为 1，单位“处” |
| 伤口总长度 | 必填数值，最小为 0，单位“厘米” |
| 期望上门时间 | 必填日期时间 |
| 预计工期 | 必填整数，最小为 1，单位“天” |
| 上门地址 | 必填详细地址，MVP 手动输入 |
| 师傅等级与人数 | 金、银、铜分别使用步进器，单级 0-20 人，总人数至少 1 人 |
| 公司名称 / 用户名字 | 必填 |
| 微信号 / 微信昵称 | 必填，用户手动输入 |
| 联系电话 | 必填，支持中国大陆手机号及业务固定电话格式 |

提交成功后创建订单并进入“待报价”。正式系统应生成唯一订单号、保存上传文件，并在客户端订单页立即可见。

### 7.2 客户订单页

顶部分类固定为：

- 待报价
- 待确认报价
- 待派单
- 施工中
- 待验收
- 已完成

除“已完成”外，每个分类直接展示该分类下所有订单的完整详情卡片，不增加“简略列表 + 同页详情”结构。完整卡片统一展示：维修内容、上门时间、服务师傅、订单金额、当前进度和当前状态可执行操作。

“已完成”分类只展示简略条目，点击后进入独立订单详情页。已完成详情展示订单号、维修内容、上门时间、服务师傅、客户订单金额、完工时间、验收结果、客户付款状态和完工凭证。

待报价、待确认报价、待派单以及已派单但尚未施工的订单允许客户提交取消申请。取消申请进入管理端“异常”Tab，必须由管理员确认后订单才变为已取消；已取消订单在客户端“已完成”分类保留记录并显示“已取消”。

客户订单详情不得展示：

- 师傅金额和平台管理费
- 师傅信用分及推荐排序
- 其他客户或其他师傅信息
- 管理员内部备注和款项操作按钮

### 7.3 报价确认

待确认报价卡片展示维修报价、上门费、报价合计和报价说明。

- “对报价有疑问”：打开平台沟通入口，不改变订单状态。
- “确认报价”：只能确认最新有效报价；确认后锁定订单金额和客户选择的师傅等级人数，订单进入待派单。
- 客户确认报价后，不允许管理员直接修改已确认金额。如需改价，必须创建新报价版本并重新取得客户确认。

### 7.4 客户验收

待验收订单点击“验收此订单”进入独立验收页。页面必须展示：

- 订单号、维修内容、上门时间、服务师傅、订单金额和施工进度
- 当前完工轮次的师傅图片、视频
- 师傅完工文字说明
- 通过验收 / 不通过选择

通过验收：

- 不要求客户再次上传文件或说明。
- 客户确认后订单进入已验收，生成客户收款和师傅付款待办。

不通过验收：

- “提交验收问题”区域自动展开。
- 验收图片和视频合计至少一项，必填。
- 问题说明必填。
- 提交后生成验收问题轮次，进入管理端“异常”队列；客户端订单显示待返修并归入施工中分类。

每次返修后，客户必须针对最新完工轮次重新验收；历史完工和验收凭证不得被覆盖。

### 7.5 我的

客户可维护：

- 我的名字 / 公司名称
- 微信号 / 微信昵称
- 联系电话
- 常用上门地址

以上字段均必填。联系电话需校验格式。资料更新后，后续新订单自动带入最新资料；历史订单保留提交时的客户信息快照。

“我的订单历史”与订单页必须读取同一数据源，不得维护另一份静态状态。联系客服用于报价疑问、派单进度、验收争议和合作咨询。

## 8. 管理端需求

### 8.1 订单工作台

顶部状态按钮固定为：

- 待报价
- 待派单
- 待施工
- 施工中
- 待验收
- 已验收

每个按钮显示订单数量并可点击筛选。管理端“待报价”包含“待平台报价”和“待客户确认报价”两个子状态。

搜索支持订单号、客户 / 联系人、电话、微信号、材质和修复类型。

订单卡片展示：订单号、客户、状态、材质 / 类型、伤口数量与长度、订单金额、师傅金额、客户确认的师傅等级人数；已验收订单额外展示款项状态。点击订单卡片或状态操作按钮进入独立订单详情页。

订单详情展示：

- 订单号、客户、微信 / 电话
- 材质、修复类型、伤口数量和长度
- 期望上门时间、预计工期、上门地址
- 客户上传的图片和视频
- 客户确认的师傅等级人数
- 客户订单金额、师傅金额
- 当前状态对应的处理区

### 8.2 报价

管理员可填写：维修报价、上门费、报价说明。

- 保存草稿：仅保存，不通知客户，不改变状态。
- 提交报价：维修报价必须大于 0；上门费不得小于 0；生成新报价版本并通知客户。
- 报价合计 = 维修报价 + 上门费。
- 客户确认前可以修改报价；修改后旧版本作废，订单回到待报价子状态。
- 客户确认后订单自动进入待派单，管理员不能越过客户确认直接派单。

参考价格只帮助管理员定损，不自动生成最终报价。

### 8.3 派单

派单前置条件：

- 客户已确认报价。
- 订单金额已锁定。
- 客户确认的金、银、铜师傅人数已锁定。

管理员只能在对应等级中选择具体师傅。例如客户确认金牌 2 位，则必须从金牌候选中选择且只能选择 2 位。每位候选师傅一行，显示序号、完整姓名、订单时段可用状态，并提供两个明确区分的操作：选择师傅、拨打电话。

保存派单设置允许暂未选满，但不得超过客户确认人数、不得跨等级选择、不得重复选择同一师傅，且未审核通过的师傅不能保存为候选。确认派单时，各等级实际选择人数必须与客户确认人数完全一致，否则阻止提交。

确认派单后：

- 创建每位师傅的分配记录。
- 订单进入管理端待施工。
- 对应师傅端出现待预约订单。
- 客户端归入施工中分类，并显示“待预约”以及已安排的师傅姓名。

### 8.4 师傅推荐排序

先按客户确认的等级过滤，再按以下顺序排序：

1. 订单上门时段可用状态：有空优先，未知其次，无空最后。
2. 公平分配：已分配订单和结算记录总数较少者优先。
3. 信用分：前两项相同时，信用分高者优先。
4. 姓名：仍相同时按姓名排序，保证结果稳定。

推荐排序只改变候选列表顺序，不自动选择或派单。管理员必须电话确认并手动勾选。

### 8.5 施工跟进

管理端展示订单金额、师傅金额、客户确认人数、执行师傅、计划上门时间和施工进度。

待施工订单包含待预约和已预约两个子状态。任一执行师傅完成到场打卡后，订单进入施工中。多师傅订单需要保留每位师傅自己的预约和打卡记录。

正常路径由师傅端提交完工凭证后自动进入待验收。管理端“完工并发起验收”在正式 MVP 中仅作为补录 / 异常处理入口，使用前必须确认已有完工凭证，并记录操作管理员和原因，不能生成没有凭证的待验收订单。

### 8.6 异常、验收争议与返修

管理端“待验收”订单用于查看客户是否已确认、联系客户和发送验收提醒。

管理端底部 `异常` Tab 处理两类异常：客户取消、客户验收不通过。列表按异常类型和处理状态分类，展示订单号、客户、异常原因、相关凭证、原订单状态和原执行师傅。

客户取消异常允许管理员：

- 联系客户，不改变订单状态。
- 确认取消：仅限待报价、待派单、待施工订单；填写原因后进入已取消。
- 保留订单：驳回取消申请，订单回到发起取消前的状态。

验收不通过异常允许管理员：

- 联系客户，不改变状态。
- 安排返修：默认沿用原执行师傅和原订单金额，生成新返修轮次，订单进入施工中。
- 驳回异常并强制完成：管理员必须填写处理说明；系统将本轮验收标记为“管理员强制完成”，订单进入已验收及收付款流程，并通知客户和执行师傅。

“驳回异常并强制完成”属于敏感操作，只允许管理员角色执行并写入不可删除的审计日志。

### 8.7 已验收与款项

客户通过验收后，订单进入“已验收”。订单款项状态分为：

- `收/付款未完成`：客户收款或任一师傅付款未全部完成。
- `收付款已全部完成`：客户应收已全部收取，且全部师傅应付已全部支付。

管理员订单详情提供两个独立按钮：

- `确认已从客户收款`
- `确认已支付给师傅`

两个按钮均为人工确认，验收通过后不得自动标记。按钮操作必须记录管理员、时间和金额，重复点击应幂等。

### 8.8 师傅管理

师傅模块首先展示待审核注册数量，并支持 `待审核 / 全部 / 金牌 / 银牌 / 铜牌` 分类。待审核卡片展示师傅提交的完整注册资料和提交时间。

管理员可对待审核师傅执行：

- 通过注册：注册状态改为已通过，保持默认铜牌，进入铜牌列表及派单候选池。
- 拒绝注册：必须填写原因，师傅端显示审核未通过；被拒绝师傅不得进入派单候选池。

已通过师傅卡片展示姓名、等级、擅长能力、接单状态、电话、常住地址、微信号、信用分和需要支付金额。

点击卡片进入独立师傅详情。基础资料以文本展示，不使用禁用输入框。管理员可查看：

- 姓名、性别、微信号、联系电话
- 城市、区 / 县、街道
- 个人介绍、能力标签、接单状态
- 被分配、进行中、完成订单
- 需要支付、已支付金额

暂时只允许编辑：

- 注册审核状态：仅待审核记录可通过或拒绝
- 师傅等级：金 / 银 / 铜下拉选择
- 信用分：0-100
- 已支付金额

保存后必须同步更新师傅列表、详情、派单候选池和结算汇总。师傅详情中的订单卡片点击后进入订单详情，返回时回到该师傅详情。

师傅详情的金额必须由每笔“订单 + 师傅”的结算记录汇总。若管理员直接编辑累计已支付金额，系统按结算记录创建时间从早到晚依次冲抵，并保留调整日志；不得只改汇总数而不生成明细变化。

### 8.9 客户管理

客户列表卡片可点击。客户详情允许管理员维护：

- 公司名称 / 用户名字
- 联系人
- 客户类型
- 微信名
- 联系电话
- 常用地址
- 客户备注

保存后列表和详情同步更新。客户订单数从订单数据实时统计，不允许手工填写。导出客户记录属于管理权限操作，必须控制权限并记录导出日志。

## 9. 师傅端需求

### 9.1 注册 / 完善资料

字段：姓名、性别、微信号、手机号、城市、区 / 县、街道、个人介绍。

- 姓名、性别、微信号、手机号、城市、区 / 县和街道必填。
- 个人介绍选填。
- 常住地址三个字段均手动输入，MVP 不自动获取当前位置。
- 新注册师傅默认铜牌且状态为待审核；审核期间只能查看或修改自己的注册资料，不能查看订单。
- 管理员通过注册后，师傅状态变为已通过，才能进入派单候选池并查看被分配订单。
- 管理员拒绝注册时，师傅端显示拒绝原因，允许修改资料后重新提交审核。
- 金牌、银牌只能由管理员调整。
- 手机号只做格式校验，不进行短信验证码验证；账号身份以微信登录返回的身份标识为准。

### 9.2 可用时间

展示未来连续 7 天，每天分上午、下午。状态点击循环：

`未知 → 有空 → 无空 → 未知`

可用时间非必填，保存后供管理端派单排序使用。过期日期不再参与推荐；正式系统每天滚动补充新的第 7 天。

### 9.3 师傅订单分类

顶部分类：

- 待预约
- 施工中
- 待验收
- 已完成

只展示分配给当前师傅的订单。所有分类的基础字段顺序统一为：

1. 上门时间
2. 预计工期
3. 应收金额
4. 地址
5. 备注
6. 到场打卡

仅“已完成”订单在应收金额之后追加“实收金额”。未打卡订单显示“未打卡”。

师傅端“应收金额”指该师傅在该订单中的应付分配金额，不是客户订单金额，也不是多人订单的全部师傅金额总池。

已取消订单归入“已完成”历史分类，但状态必须显示“已取消”，卡片覆盖明显的“已取消”水印。预约、打卡、上传和提交按钮保留为灰色禁用状态，不允许继续操作。

### 9.4 预约、电话与打卡

- 拨打客户：调用微信拨号能力，电话号码来自订单客户快照。
- 已预约：只在待预约状态可操作，操作前师傅应已电话确认客户。
- 到达打卡：待预约状态不可操作。点击后必须先调用微信定位能力并请求用户授权。
- 定位成功：保存服务器打卡时间、经纬度和定位精度，订单展示定位结果及时间。
- 定位失败、用户拒绝授权或系统无法定位：仍然允许打卡成功，保存定位失败原因，订单打卡信息统一显示为“（无定位）YYYY-MM-DD HH:mm”。
- MVP 不校验师傅与上门地址之间的距离，不因超出距离阻止打卡。
- 同一订单、同一师傅只允许一条有效到场记录；重复点击返回已有结果。

### 9.5 完工提交

师傅可上传多张图片和视频，并填写完工文字说明。提交规则：

- 完工图片必填，至少 1 张、最多 5 张。
- 完工视频必填，至少 1 个、最多 2 个。
- 任一项数量不符合要求时不得提交完工。
- 文字说明当前为选填，但提交后必须在客户验收页展示。
- 提交成功后生成新的完工 / 验收轮次，订单进入待验收。
- 返修完工必须新增凭证轮次，不能覆盖首次施工凭证。

媒体格式、单文件大小、压缩与转码参数由技术设计根据微信接口和对象存储限制确定，但不得改变上述图片和视频数量规则。完工与验收凭证随订单长期留存，具体数据删除和法定保留期限以隐私政策为准。

### 9.6 师傅金额

- 所有师傅订单显示应收金额。
- 单师傅订单可在派单后直接显示系统计算的应收金额。
- 多师傅订单在完工前显示“应收金额：待分配”；完工后由管理员手工分配，保存后各师傅只看到自己的应收金额。
- 仅已完成订单显示实收金额。
- 未完成订单不显示实收金额字段。
- 实收金额仅来源于管理员确认的师傅付款记录。
- 客户验收通过只会让订单进入已完成，不会自动产生实收金额。

## 10. 金额与结算规则

### 10.1 金额口径

- 维修报价：管理员填写的维修服务费，不含上门费。
- 上门费：单独填写，默认参考 50 元，但管理员可根据实际情况调整。
- 客户订单金额：维修报价 + 上门费，客户确认后锁定。
- 师傅金额总池：客户订单金额的 90%，按元四舍五入。
- 平台管理费：客户订单金额 - 师傅金额总池。

管理端订单卡片和详情在“订单金额”下一行显示“师傅金额”。客户端只显示客户订单金额。师傅端只显示分配给当前师傅的应收和实收金额。

### 10.2 多师傅订单

多人订单的 90% 是整笔订单的师傅金额总池，不是每位师傅各得 90%。多人订单派单时只确定具体师傅，不分配个人金额；师傅提交完工后，由管理员手工为每位执行师傅分配应收金额。

管理端在完工后的订单详情中提供“分配师傅金额”区域，列出所有执行师傅、等级和金额输入框。保存时校验：

- 每位师傅金额不得小于 0。
- 所有执行师傅应收金额之和必须等于师傅金额总池。
- 金额未分配完成时不得确认师傅付款。
- 客户端不得看到师傅间的金额分配。
- 金额保存后生成每位师傅独立的应付记录；后续修改必须保留调整历史。

当前小程序实现已在管理端订单详情提供多人金额分配控件；正式开发时必须延续上述总额校验、权限边界和调整历史要求。

### 10.3 收付款状态

- 客户应收金额默认为客户订单金额。
- 客户已收金额由管理员人工确认记录。
- 每位师傅应付金额为该师傅的分配应收金额。
- 每位师傅已付金额由管理员人工确认记录。
- 支持未完成、部分完成、已完成三种金额状态。
- 汇总金额必须由明细计算，不能把汇总字段作为唯一事实来源。

MVP 不接入自动微信支付或自动分账。客户端宣传中的“微信协同结算”表示沟通和记录协同，不代表本期已实现微信支付。

## 11. 跨端同步与通知

三个端必须使用同一订单号和同一后端状态机。任何操作成功后，相关端下次进入页面或主动刷新时必须获取最新状态；关键操作建议同时触发订阅消息。

建议通知事件：

- 管理员提交报价：通知客户确认。
- 师傅提交注册：通知管理员审核。
- 管理员通过或拒绝师傅注册：通知师傅审核结果。
- 客户确认报价：通知管理员进入派单。
- 管理员派单：通知被选师傅和客户。
- 师傅确认预约：通知客户和管理员。
- 师傅到场打卡：通知管理员。
- 师傅提交完工：通知客户验收和管理员跟进。
- 客户验收不通过：通知管理员和相关师傅。
- 管理员安排返修：通知客户和师傅。
- 管理员驳回异常并强制完成：通知客户和师傅，并说明处理结果。
- 管理员确认取消：通知客户；已经派单时同时通知所有执行师傅。
- 客户验收通过：通知管理员和师傅。
- 管理员确认师傅付款：更新师傅端实收金额并通知师傅。

消息发送失败不得回滚已经成功的核心业务状态，但必须记录失败并允许重试。

## 12. 权限、隐私与审计

### 12.1 权限

- 客户：只访问自己的资料和订单。
- 待审核师傅：只访问自己的注册资料和审核结果。
- 已通过师傅：只访问自己的资料、可用时间、分配订单和款项。
- 管理员：必须命中服务器端 `unionid/openid` 白名单，并按后台角色授权访问订单、异常、客户、师傅和款项操作。
- 前端显示管理端页面不能替代服务器鉴权；每个管理接口都必须重新校验管理员角色。
- 导出、注册审核、强制完成、信用分修改、等级修改、金额确认属于敏感操作，应限制给管理员并记录审计。

### 12.2 隐私

- 上传媒体、手机号、微信号和地址均属于敏感信息。
- 媒体文件应使用私有对象存储和带时效的访问地址。
- 客户与师傅的联系方式只向当前订单必要角色展示。
- 删除账号、数据保留期限和隐私政策需在正式开发前形成合规方案。

### 12.3 审计日志

以下操作必须记录操作者、时间、操作前后值和订单 / 对象 ID：

- 报价提交、修改、客户确认
- 师傅注册审核、派单、改派、师傅等级和信用分调整
- 到场打卡及定位结果、完工提交、客户验收、安排返修
- 客户取消、管理员取消、保留订单、驳回异常并强制完成
- 客户收款和师傅付款确认
- 客户、师傅资料修改及数据导出

## 13. 非功能要求

- 移动端优先，以 390px 宽度为核心适配基准，并兼容常见微信小程序屏幕宽度。
- 主要按钮点击区域不小于 44px 高。
- 上传操作必须展示进度、失败原因和重试入口。
- 状态变更按钮提交期间禁用，避免重复操作。
- 所有金额由后端使用整数分存储，前端仅格式化展示。
- 所有业务时间由服务器生成并保存时区，前端按 `Asia/Shanghai` 展示。
- 列表需要分页或增量加载；搜索和筛选在正式数据量下仍可用。
- 核心状态变更、金额更新和文件归属必须有后端权限校验，不能只依赖前端隐藏按钮。
- 订单、报价、派单、验收和支付接口必须支持幂等。
- 客户和师傅手机号不使用短信验证码；手机号格式校验与微信登录身份校验相互独立。

## 14. MVP 验收标准

### 14.1 客户端

- 能按校验规则提交完整询价并生成待报价订单。
- 六个订单分类数量与订单状态一致。
- 能确认最新报价，订单跨端进入待派单。
- 能查看完工凭证并通过验收。
- 不通过时必须上传凭证并填写说明，随后进入返修流程。
- 已完成订单可查看独立详情，且不泄露内部金额和派单信息。

### 14.2 管理端

- 待报价、待派单、待施工、施工中、待验收、已验收六个订单分类及搜索结果正确。
- 报价草稿、提交、修改、客户确认后的状态流转正确。
- 派单只能选择客户确认等级中的师傅，且人数必须精确匹配。
- 候选排序遵循可用状态、公平分配、信用分、姓名的顺序。
- 待审核师傅只有通过注册后才能进入派单候选池。
- “异常”Tab 能处理客户取消、安排返修以及驳回异常并强制完成。
- 客户收款和师傅付款必须分别手动确认。
- 客户和师傅资料修改后，相关列表、详情和派单候选同步更新。

### 14.3 师傅端

- 可填写未来 7 天上午 / 下午状态，未填写显示未知。
- 审核通过后只看到分配给自己的订单；待审核或已拒绝时不能进入订单功能。
- 可拨打客户、确认预约并完成到场打卡。
- 定位失败时仍能完成打卡，并显示“（无定位）时间”。
- 每次完工必须上传 1-5 张图片和 1-2 个视频。
- 完工凭证和文字说明能在客户端验收页看到。
- 所有订单显示应收金额，只有已完成订单显示实收金额。
- 已取消订单显示水印，全部履约按钮为灰色禁用状态。

### 14.4 跨端

- 同一订单在三个端的订单号、金额和主状态一致。
- 客户确认报价、管理员派单、师傅完工、客户验收和管理员付款均能正确推动相关端状态。
- 重复提交不会生成重复记录。
- 所有关键操作可从审计日志追溯。

## 15. 已确认实施决策与本期边界

### 15.1 本期不包含

- 自动无人值守派单、拒单超时自动顺延
- 微信支付、退款、自动分账和发票
- 自动获取客户或师傅真实微信号
- 师傅常住地址自动定位
- 自动判断施工质量或自动验收
- 复杂优惠、合同、账期和授信体系

### 15.2 已确认实施决策

- 客户端、师傅端、管理端使用三个独立小程序 AppID，共用一个后端，并绑定同一个微信开放平台账号。
- 管理员通过服务器端 `unionid/openid` 白名单授权；手动提供的真实微信号仅用于人工核对，不能作为小程序登录标识。
- 师傅通过师傅端注册，默认铜牌且为待审核；管理员在师傅模块通过注册后，师傅才能进入派单候选池。
- 完工每轮必须上传 1-5 张图片和 1-2 个视频。
- 到场打卡必须尝试获取定位；定位失败仍打卡成功，并显示“（无定位）时间”。本期不做距离围栏。
- 多师傅订单在完工后由管理员手工分配每位师傅的应收金额。
- 管理端增加待施工分类；管理员可取消待报价、待派单、待施工订单。
- 已经派给师傅的订单被取消后，师傅端显示“已取消”水印，全部履约按钮禁用。
- 管理端底部使用“异常”Tab，处理客户取消和验收不通过。
- 管理员可以驳回验收异常并强制完成订单，必须填写处理说明并记录审计日志。
- 客户和师傅手机号均不进行短信验证，只做格式校验。

文件格式、单文件大小、压缩、转码及对象存储生命周期属于技术设计参数，由技术方案在不改变上述产品规则的前提下确定。

## 16. 原型与文档对应

- `index.html`：客户端原型
- `admin.html`：管理端原型
- `master.html`：师傅端原型
- `outputs/`：对外预览副本

正式开发开始后，原型用于视觉与交互参考，本 PRD 用于判断业务行为；若两者冲突，以最新确认的 PRD 和后续变更记录为准。

当前三端静态原型尚未全部反映 1.1 版新增规则。正式开发或下一轮原型更新时，必须补齐：

- 管理端底部导航将“验收”改为“异常”，并实现客户取消、验收不通过、安排返修和驳回异常强制完成流程。
- 管理端订单工作台增加“待施工”分类，师傅到场打卡后才转入“施工中”。
- 管理端师傅模块增加待审核列表、通过和拒绝操作，未通过师傅不进入派单候选池。
- 管理端多师傅订单增加完工后的师傅应收金额分配控件与总额校验。
- 客户端增加可取消阶段的取消申请入口，师傅端增加已取消水印和全部履约按钮禁用状态。
- 师傅端增加注册审核中、审核未通过和审核通过状态，并按本 PRD 补齐打卡定位结果、完工媒体数量和多师傅金额待分配状态。
